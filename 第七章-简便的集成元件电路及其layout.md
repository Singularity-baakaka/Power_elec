
前六章已经介绍了开关器件、分析方法、基础拓扑、磁件以及控制与驱动。

为了方便原理、底层知识的学习，前六章是基于分立元件进行讲解的。然而，在实际的工程应用中使用分立元件其实是低效、不常见的。使用集成元件是更为合理的，学习使用集成电源芯片进行设计在实践中是必要的。

本章将在前文知识的基础上，说明如何使用集成电源芯片简化分立电路，并以 Buck 变换器为例介绍原理图和 PCB Layout 的基本设计流程。

# 7.1 从分立变换器到集成电源芯片

## 7.1.1 集成电源芯片代替了什么

集成元件相当于是一整块电路的集合，相当于将较为复杂的电路合体放置于一小块元件中，使得整体电路的设计更加简单，一定程度上也可能更加可靠。

不同程度的集成可能意味更方便的设计，但是同时也可能意味着可调性更小，同时发热控制、合理的封装选择等也是值得考虑的因素，无脑地选择高集成度的芯片是不可取的。

另外，值得注意的是，当代芯片的制造工艺决定了在较小的封装生产半导体器件比大电阻/电容相对更加的容易，理解这一点后便容易理解一些芯片内部经常使用半导体器件在某种状态下的作用来代替电阻/电容或者要求连接外置电阻电容的设计。

总而言之，集成芯片改变的是电路的实现边界，并不改变第四章所讲的开关状态和能量传递规律。

## 7.1.2 电源芯片的常见集成程度

不同电源芯片的集成程度有高有低，相当于芯片内部电路边界不同，对于电源芯片，常见的几种集成程度有。

### 7.1.2.1 仅集成控制器

**芯片内部通常集成：**

- 基准电压源；
- 振荡器或时钟电路；
- 误差放大器；
- PWM 比较器；
- 电流检测与限流逻辑；
- 软启动、欠压锁定和故障逻辑。

**芯片外部通常需要：**

- 栅极驱动器；
- MOSFET 或 IGBT；
- 续流二极管或整流器；
- 电感、变压器等磁性元件；
- 输入、输出电容；
- 反馈、补偿和保护元件。

**优点：** 功率器件可以根据耐压、电流、频率和散热条件自由选择，适合大功率、高电压和特殊拓扑。

**缺点：** 外围器件较多，驱动回路和功率回路需要自行设计，PCB 面积、调试难度和设计风险较高。

**常见器件举例：** UC3842/UC3843 等电流模式控制器常用于 Flyback 和 Forward；SG3525、TL494 等 PWM 控制器常用于推挽、半桥和全桥类电路；LM3478 等控制器可用于 Boost。

### 7.1.2.2 集成控制器和栅极驱动器

**芯片内部通常集成：**

- 控制器；
- 低边或高低边栅极驱动器；
- 电平转换电路；
- 死区控制；
- 驱动欠压锁定和部分保护逻辑。

**芯片外部通常需要：**

- 一个或多个外部 MOSFET/IGBT；
- 栅极电阻和下拉电阻；
- 电感、变压器或其他磁件；
- 二极管、输入输出电容；
- 反馈和补偿元件。

**优点：** 驱动峰值电流、电平转换和死区控制已经由芯片处理，适合较高功率或需要自行选择功率开关的设计。

**缺点：** 功率开关仍然外置，功率回路和栅极回路的寄生参数仍需自行控制；芯片与功率器件之间的 Layout 对开关速度和 EMI 影响较大。

**常见器件举例：** LM5116 等同步 Buck 控制器可提供高低侧 MOSFET 驱动；UCC 系列中的部分半桥驱动器、隔离驱动器可用于 Half-Bridge、Full-Bridge 和 IGBT/MOSFET 驱动。具体器件应根据是否集成控制逻辑、驱动电源和保护功能进行区分。

### 7.1.2.3 集成控制器、驱动器和单个主功率开关

这类器件通常用于异步 Buck、Boost、Flyback 等电路。芯片内部将控制电路、栅极驱动器和一个主 MOSFET 集成在一起。

**芯片内部通常集成：**

- PWM、PFM 或峰值电流控制器；
- 栅极驱动器；
- 主功率 MOSFET；
- 电流检测与限流电路；
- 软启动、欠压和过温保护。

**芯片外部通常需要：**

- 电感或 Flyback 变压器；
- 续流或整流二极管；
- 输入、输出电容；
- 反馈分压和补偿网络；
- 必要的箝位、吸收和输入保护电路。

**优点：** 外围器件数量少，功率开关与驱动器之间的寄生参数由芯片内部控制，适合低成本和中小功率电源。

**缺点：** 功率 MOSFET 的耐压、导通电阻和热性能不可单独更换；内部功率开关的散热依赖芯片封装和 PCB 铜皮；电路拓扑和开关频率的可调范围也可能受到限制。

**常见器件举例：** LM2596、LM2576 属于常见的集成开关降压器件；TPS61023、TPS61088 等属于集成主开关的 Boost 类器件；部分 VIPer、Power Integrations LinkSwitch 系列器件则将主开关与 Flyback 控制功能集成在一起。

### 7.1.2.4 集成同步功率级

这类芯片在集成控制器和驱动器的基础上，同时集成高侧、低侧两个 MOSFET，形成一个内部同步半桥。

**芯片内部通常集成：**

- 控制器；
- 高侧 MOSFET；
- 低侧 MOSFET；
- 高低侧栅极驱动器；
- 死区控制和防直通逻辑；
- 电流检测、软启动和保护电路。

**芯片外部通常需要：**

- 输入电容；
- 输出电感；
- 输出电容；
- 反馈、使能和模式配置元件；
- Bootstrap 电容，若芯片没有内部集成；
- 必要的外部输入和输出保护元件。

**优点：** 外围器件少，低侧 MOSFET 的导通压降低于二极管正向压降，适合低压、大电流和高效率 Buck；高低侧驱动、死区和防直通逻辑已在芯片内部完成。

**缺点：** 功率 MOSFET 不能单独更换；芯片的热阻和最大输出电流受封装限制；输入去耦、SW 节点、电感和反馈 Layout 仍然非常关键。

**常见器件举例：** TPS62160、TPS5430/部分 TPS 系列、MP1584、MPM 系列中的部分 Buck 器件可作为集成式 Buck 的例子。具体型号应根据其是否集成低侧 MOSFET、是否需要外部二极管以及数据手册的功能框图进行确认。

### 7.1.2.5 集成电感或磁件的电源模块

电源模块在集成控制器、驱动器和功率开关的基础上，进一步将电感或其他磁性元件封装在模块内部，少数隔离模块还会集成变压器和整流器。

**芯片或模块内部可能集成：**

- 控制器；
- 栅极驱动器；
- 功率 MOSFET；
- 电感；
- 补偿网络；
- 部分输入、输出滤波元件。

**外部通常只需要：**

- 输入、输出电容；
- 反馈或输出设置电阻；
- 使能、软启动和同步元件；
- 必要的外部保护器件。

**优点：** 外围器件最少，磁件参数和高频功率回路已经由厂商优化，设计周期短，适合空间受限和快速开发的场合。

**缺点：** 成本较高，输出电流、输入输出范围和散热能力受模块型号限制；磁件不可更换，Layout 需要严格遵循模块的焊盘、散热和禁布区域要求。

**常见器件举例：** TI LMZ、TPSM 系列，ADI/LT LTM 系列以及 MPS MPM 系列中均有集成电感或磁件的电源模块。选型时需要确认型号是否真正集成电感，不能仅根据“Power Module”名称判断。

### 7.1.2.6 集成在电源芯片中的附加功能

除了控制器、驱动器和功率开关，一些电源芯片还集成了保护、启动、监测和通信辅助电路。它们不一定改变主功率拓扑，但会影响芯片的外部连接和系统行为。

**常见保护功能：**

- 输入欠压锁定 UVLO；
- 输入过压保护；
- 逐周期过流限制；
- 短路保护；
- 输出过压保护；
- 过温关断；
- 反向电流限制；
- 输出放电；
- 打嗝重启或锁存关断。

**常见启动与控制辅助功能：**

- 软启动；
- 输出跟踪；
- 使能控制；
- 电源正常指示 PGOOD；
- 频率设定与外部同步；
- PWM、PFM、Burst 和强制连续模式选择；
- 展频以降低 EMI 峰值。

**常见内部模拟功能：**

- 内部反馈基准；
- 内部补偿网络；
- 电流检测放大器；
- 前沿消隐；
- 斜坡补偿；
- 热关断检测；
- 高边 Bootstrap 供电与欠压检测。

这些功能虽然可以减少外围电路，但并不意味着设计者可以忽略数据手册。需要重点查看：

- 保护阈值及其容差；
- 保护触发后的恢复方式；
- 保护状态下的最大电流和最大功耗；
- 外部引脚是否需要上拉、下拉或定时电容；
- 芯片是否允许在轻载时反向传能；
- 内部补偿所允许的电感和输出电容范围。

> “集成了保护功能”与“系统不需要外部保护”不是同一件事。输入反接、浪涌、接口 ESD、输出反灌、保险丝和安规保护仍应根据应用环境单独评估。

### 7.1.2.7 不同集成程度的综合比较

| 集成程度 | 主要集成内容 | 典型外部器件 | 主要优点 | 主要限制 |
|---|---|---|---|---|
| 仅集成控制器 | PWM、反馈、保护逻辑 | 驱动器、MOSFET、磁件、二极管、电容 | 灵活、适合高功率 | 外围多、设计复杂 |
| 控制器+驱动器 | 控制器、驱动、电平转换 | MOSFET、磁件、二极管、电容 | 驱动简单、功率器件可选 | 功率回路仍需自行设计 |
| 控制器+单主开关 | 控制器、驱动、主 MOSFET | 磁件、二极管、电容 | 成本低、外围少 | 开关性能和散热受内部器件限制 |
| 控制器+同步功率级 | 控制器、驱动、高低侧 MOSFET | 电感、电容、反馈元件 | 效率高、外围少 | Layout 和散热要求较高 |
| 电源模块 | 控制器、功率级、电感等 | 少量电容和设置元件 | 开发快、尺寸小 | 成本高、可调性和可维修性较低 |

### 7.1.2.8 选择集成程度时的判断原则

- 功率越大、输入电压越高，越需要保留外部功率器件的选择空间；
- 体积越小、开发周期越短，越适合选择集成开关或电源模块；
- 低压大电流场合通常优先考虑同步功率级；
- 高压、隔离和桥式拓扑通常更多采用控制器、驱动器和外部功率器件；
- 需要特殊开关频率、软开关或定制磁件时，不宜只追求最高集成度；
- 集成度越高，越要重视封装热阻、推荐 Layout 和芯片内部功率路径；
- 保护功能应与系统级保护一起评估，不能只根据功能列表选型。
## 7.1.3 各拓扑常见集成方案对照表

| 拓扑 | 常见集成器件 | 主要代替部分 | 通常仍需外置 |
|---|---|---|---|
| Buck | 控制器、异步/同步稳压器、电源模块 | PWM、驱动、功率开关、保护 | 电感、电容、反馈元件 |
| Boost | 控制器、集成开关转换器、同步转换器 | PWM、驱动、主开关、同步整流 | 电感、电容，异步型二极管 |
| 反相 Buck-Boost | 专用反相转换器、可配置稳压器 | 控制、驱动和功率开关 | 电感、电容、反馈元件 |
| Flyback | 控制器、集成主开关芯片、隔离模块 | PWM、驱动、主开关、保护 | 耦合磁件、整流、箝位和电容 |
| Forward | Forward 控制器、有源箝位控制器 | PWM、驱动逻辑和保护 | 开关、变压器、复位、整流和滤波 |
| Half-Bridge | 半桥驱动器、控制器、功率模块 | 驱动、死区、保护和功率开关 | 母线电容、变压器或负载 |
| Full-Bridge | 全桥控制器、多路驱动器、功率模块 | 调制、驱动、死区和功率开关 | 母线电容、变压器、整流和滤波 |

## 7.1.4 本章选择 Buck 作为案例

后文将以经典的低压集成式 Buck 为例，讲解从设计需求、芯片选型和外围器件计算，到原理图与 PCB Layout 的完整流程。

# 7.2 集成 Buck 变换器的原理图设计

在使用集成 Buck 芯片进行设计时，不能只根据输入电压、输出电压和输出电流直接照搬数据手册中的典型应用电路。

典型应用电路可以帮助设计者快速理解芯片的基本连接方式，但是其中的输入条件、负载范围、纹波要求和散热条件不一定与实际需求相同。即使芯片型号相同，输入电压范围、输出电流或者开关频率发生变化，所需要的电感和电容也可能不同。

因此，集成 Buck 变换器的原理图设计仍然需要经过以下过程：

设计需求
→ 芯片选型
→ 理解典型应用电路
→ 连接反馈和辅助引脚
→ 选择电感与电容
→ 汇总并检查原理图

本节将先说明各步骤中的通用判断方法，再以 TPS54302 同步 Buck 芯片为例完成一套具体设计。案例中的数值不能直接套用到其他芯片，但分析和校核过程可以作为类似设计的参考。

## 7.2.1 明确设计需求

在选择芯片和外围元件之前，首先需要明确变换器需要在什么条件下工作。

如果只给出“将 12 V 转换为 5 V”，那么设计条件实际上是不完整的。实际输入并不会始终等于标称值，负载电流也会发生变化。设计时应当使用输入电压范围和负载范围进行校核，而不能只计算某一个标称工作点。

本节贯穿案例的基本需求如下表所示。

| 项目 | 设计要求 |
|---|---|
| 标称输入电压 | 12 V |
| 输入电压范围 | 9～16 V |
| 输出电压 | 5 V |
| 最大输出电流 | 2 A |
| 最大输出功率 | 10 W |
| 电感电流纹波目标 | 最大输出电流的约 30%，即约 0.6 A |
| 稳态输出电压纹波目标 | 不超过 50 mV |
| 变换器类型 | 非隔离同步 Buck |

输入电压范围会影响芯片耐压、占空比以及电感电流纹波。对于 Buck 变换器，最高输入电压通常对应更大的电感电流纹波，因此不能只在 12 V 标称输入下进行计算。

最大输出电流会影响芯片、电感和输出电容的选择。芯片的额定输出电流不宜刚好等于设计所需的最大电流，还需要考虑限流阈值、开关管损耗、环境温度和 PCB 散热条件。

本案例将电感电流纹波初步设为最大输出电流的约 30%。这个数值不是必须满足的固定标准，而是在电感体积、峰值电流、损耗和输出纹波之间所作的折中。实际选用标准感量后，还需要重新计算真实的电流纹波。

输出电压纹波目标暂定为 50 mV。这里主要指稳定工作时的开关纹波，不等同于负载突变时的瞬态偏差。输出电容的 ESR、有效容量、PCB Layout 和测量方法都会影响最终测得的纹波，因此该目标只能作为原理图设计阶段的选型依据，最终结果仍需要通过实物测量确认。

除了表中的电气指标，实际产品还应明确：

- 环境温度范围；
- 允许的 PCB 面积；
- 散热条件；
- 效率要求；
- 待机功耗要求；
- 输入电源的浪涌和反接风险；
- 成本与器件供应要求。

本案例的重点是低压集成 Buck 的基本原理图设计，不针对汽车电源、长线输入或其他强浪涌环境，也不对外部接口保护进行具体设计。

> 标称工作条件适合用于描述电路，输入范围和负载范围才是器件选型与最差工况校核的依据。

## 7.2.2 根据需求选择芯片
在实际的设计过程中，进行初步搜索-确定候选芯片-数据手册核对是常见的选型步骤。进行初步搜索的方式是多样的，部分元件选型和采购网站提供按参数筛选功能，另外使用人工智能进行快速检索也是一种不错的方案。

根据上述设计需求，经过初步搜索后，发现使用TPS54302比较合适，这也是一款常用的电源芯片，接下来将根据其数据手册详细核对其参数并逐步确定其作为本案例使用的核心芯片是否完全合适。

![[tps54302.pdf]]

对于本案例，首先需要检查输入电压范围。TPS54302 的推荐输入电压范围为 4.5～28 V，可以覆盖 9～16 V 的实际输入范围。其 VIN 引脚绝对最大额定值为 30 V，但是绝对最大额定值不能作为正常工作的设计值。16 V 与 28 V 推荐工作上限之间保留了一定余量，因此在输入电源本身受控、没有强浪涌的情况下，该耐压范围适合本案例。

> 推荐工作范围说明芯片能够正常工作的条件，绝对最大额定值只表示器件不应被超过的边界，两者不能混用。

TPS54302 的连续输出电流能力为 3 A，而本案例最大负载电流为 2 A。从额定值上看留有 1 A 余量，但这并不意味着任何 PCB 和环境温度下都可以稳定输出 3 A。芯片内部 MOSFET 的导通损耗、开关损耗以及封装向 PCB 的散热能力，都会限制实际可用的输出电流。

TPS54302 内部集成了高侧和低侧 N 沟道 MOSFET，属于同步 Buck 芯片。与使用外部续流二极管的异步 Buck 相比，低侧 MOSFET 的导通压降较低，在 5 V、2 A 这类低压输出条件下通常有利于提高效率。

该芯片采用峰值电流模式控制，标称开关频率为 400 kHz，并已经集成环路补偿网络。较高的开关频率可以减小电感和电容，但会增加开关损耗。内部补偿减少了外围元件数量，不过外部电感和输出电容仍然需要处于合理范围内。

TPS54302 使用 6 引脚 SOT-23-THIN 封装，外围连接较少，适合作为基础的集成 Buck 案例。它还具有内部软启动、轻载跳脉冲模式以及多种内部保护功能。

根据以上条件，可以得到以下校核结果。

| 选型项目 | 案例需求 | TPS54302 的相关条件 | 判断 |
|---|---:|---:|---|
| 输入电压 | 9～16 V | 推荐工作范围 4.5～28 V | 满足 |
| 输出电压 | 5 V | 通过 FB 分压设置 | 满足 |
| 最大输出电流 | 2 A | 连续输出能力最高 3 A | 具有一定余量 |
| 整流方式 | 同步整流 | 内置高侧、低侧 MOSFET | 满足 |
| 补偿方式 | 优先内部补偿 | 内置补偿网络 | 满足 |
| 标称开关频率 | 未特别限定 | 400 kHz | 可接受 |
| 封装 | 外围简单 | 6 引脚 SOT-23-THIN | 适合教学案例 |

因此，本案例选择 TPS54302 作为主芯片。

## 7.2.3 读懂芯片的典型应用电路

选定芯片后，可以先区分哪些电路已经集成在芯片内部，哪些元件仍然需要外置。芯片内部框图通常可以在数据手册的“功能框图”（Functional Block Diagram）或“详细说明”（Detailed Description）一类章节中找到。不同厂商对章节的命名和编号可能不同。

TPS54302 内部主要包含：

- 高侧 MOSFET；
- 低侧 MOSFET；
- 高低侧栅极驱动电路；
- PWM 与峰值电流模式控制电路；
- 误差放大器和内部补偿网络；
- 电流检测、死区控制和保护逻辑；
- 内部软启动与 Bootstrap 充电二极管。

外部仍然需要：

- Bootstrap 电容；
- 输出电感；
- 输入电容；
- 输出电容；
- 输出反馈分压电阻；
- 必要的前馈电容。

从功率级看，VIN 引脚连接高侧 MOSFET 的漏极，高侧和低侧 MOSFET 的中点连接到 SW 引脚。SW 再经过外部电感连接到输出，输出端使用电容滤波。这个结构与第四章分析的同步 Buck 并没有本质区别，只是两个功率开关、驱动器和控制器被放入了同一个芯片中。

实际设计中，当我们希望快速完成设计不关注芯片内部结构时，这一步在一定程度上可以省略，从芯片的引脚定义、典型应用电路、外围元件选型说明入手也是一种常见的做法。

芯片的引脚定义通常可以在数据手册的“引脚配置与功能”（Pin Configuration and Functions）或“引脚说明”（Pin Description）一类章节中找到。

TPS54302 的六个引脚作用如下。

| 引脚 | 作用 |
|---|---|
| VIN | 芯片输入供电，同时连接内部高侧 MOSFET 的功率输入端 |
| SW | 内部上下 MOSFET 的开关节点，外接输出电感 |
| GND | 控制电路地和内部低侧 MOSFET 的功率回流端 |
| FB | 输出电压反馈输入端 |
| EN | 芯片使能控制端 |
| BOOT | 高侧 MOSFET 驱动的自举供电端 |

当引脚定义明晰后，我们便可以较好地理解芯片的典型应用电路。典型应用电路通常可以在数据手册的“典型应用”（Typical Application）或“应用信息”（Application Information）一类章节中找到。

 数据手册中图7-1是数据手册给出的 5 V/3 A 参考设计，并不是本章最终采用的 9～16 V 输入、5 V/2 A 输出电路。图中的 10 μH 电感、反馈分压、前馈电容和 EN 欠压设置网络对应数据手册的参考条件。后文将保留其基本连接关系，再根据本案例的纹波目标重新计算外围元件；同时按照前面的约定，不使用 R4、R5 对 EN 欠压门槛进行额外设置。

典型应用电路表示这些元件之间的电气关系，但不能表达元件在 PCB 上的距离和回路面积。例如，输入电容在原理图中只要连接在 VIN 和 GND 之间便是正确的，但是在 PCB 中距离芯片过远，就无法有效承担高频开关电流。这部分问题将在 7.3 中讨论。

在确定了使用的典型电路后，确定外围元件的具体取值便是接下来关键的一步。下面的 7.2.4～7.2.6 节将结合本教程前文知识和数据手册进行详细讲解。通常，在芯片数据手册的“详细设计过程”（Detailed Design Procedure）、“外围元件选择”（External Component Selection）或“应用信息”（Application Information）一类章节中，可以找到外围元件的计算公式、推荐范围和选型说明，用于快速确定初始参数。

## 7.2.4 反馈与辅助引脚的连接

### 7.2.4.1 反馈分压网络

TPS54302 通过 FB 引脚检测输出电压。芯片内部会将 FB 电压与典型值为 0.596 V 的参考电压进行比较，并据此调节开关过程。

在输出端与地之间连接分压电阻，其中上方电阻记为 $R_{FBT}$，下方电阻记为 $R_{FBB}$，则输出设定值为：

$$
V_o=V_{ref}\left(1+\frac{R_{FBT}}{R_{FBB}}\right)
$$

当目标输出电压为 5 V 时，两只电阻的比值应满足：

$$
\frac{R_{FBT}}{R_{FBB}}
=
\frac{V_o}{V_{ref}}-1
=
\frac{5}{0.596}-1
\approx 7.39
$$

先将上方电阻取为 100 kΩ，则下方电阻的理论值为：

$$
R_{FBB}
=
\frac{100\text{ k}\Omega}{7.39}
\approx 13.5\text{ k}\Omega
$$

因此，本案例暂取：

- $R_{FBT}=100\text{ k}\Omega$；
- $R_{FBB}=13.5\text{ k}\Omega$。

反算输出设定值为：

$$
V_o=0.596\left(1+\frac{100}{13.5}\right)
\approx 5.01\text{ V}
$$

实际输出还会受到参考电压误差、电阻容差、反馈走线压降和噪声的影响。因此，反馈电阻通常选择 1% 或更高精度，并应根据能够采购到的标准阻值重新计算实际输出范围。

反馈电阻不能无限减小。过小的阻值会持续消耗输出功率；但阻值也不能过大，否则 FB 引脚的输入电流和开关噪声更容易造成误差。100 kΩ 左右的上方电阻是一个常见起点。

对于 5 V 输出和低 ESR 陶瓷输出电容，数据手册还建议在上方反馈电阻两端并联一个小容量前馈电容。本案例采用 75 pF 作为起始值，介质优先选择 C0G/NP0。前馈电容会影响环路的高频响应，不能在不了解芯片补偿方式的情况下随意照搬到其他型号。

### 7.2.4.2 BOOT 自举电容

TPS54302 内部高侧开关使用 N 沟道 MOSFET。为了使高侧 MOSFET 可靠导通，栅极驱动电压需要高于不断变化的 SW 节点电压。

芯片内部已经集成 Bootstrap 充电二极管，但是仍然需要在 BOOT 与 SW 引脚之间外接一个 0.1 μF 陶瓷电容。该电容不是连接在 BOOT 与地之间，其参考点是 SW 节点。

本案例选择：

- 容量：0.1 μF；
- 介质：X7R 或 X5R；
- 耐压：不低于 10 V。

自举电容的充放电回路很短，实际 PCB 中必须靠近 BOOT 和 SW 引脚放置。

### 7.2.4.3 EN 使能引脚

EN 引脚用于控制芯片启动和停止。TPS54302 的 EN 引脚具有内部上拉电流源，在没有外部连接时，芯片默认处于使能状态。

本案例不使用 EN 电阻分压网络设置额外的输入欠压门槛，也不设计外部时序控制。因此，EN 引脚保持悬空，并在原理图中增加未连接标记和说明，避免被误认为遗漏连接。

需要特别注意，EN 引脚的推荐工作电压上限低于本案例的输入电压，不能因为需要“固定使能”便直接将 EN 连接到 9～16 V 的 VIN。

### 7.2.4.4 VIN、GND 与 SW 引脚

VIN 引脚既为芯片控制电路供电，也是内部高侧 MOSFET 的功率输入端。输入电容应连接在 VIN 与 GND 之间。

GND 引脚同时承担控制电路参考地和低侧 MOSFET 的功率回流。原理图中虽然统一使用 GND 网络，但反馈地和大电流回流在 PCB 上不能随意混合走线。

SW 引脚是内部高低侧 MOSFET 的连接点，应只连接输出电感、自举电容以及必要的测试位置。该节点具有较高的 $\mathrm{d}v/\mathrm{d}t$，容易通过寄生电容向周围电路耦合噪声。

## 7.2.5 输出电感的选择

输出电感与输出电容共同构成 Buck 的输出滤波器。电感值过小会造成较大的纹波电流和峰值电流，电感值过大则通常意味着更大的体积、直流电阻和成本，并且会改变控制环路的动态特性。

忽略损耗时，Buck 变换器在连续导通模式下的占空比近似为：

$$
D\approx\frac{V_o}{V_{in}}
$$

电感电流的峰峰值纹波可以写为：

$$
\Delta I_L
=
\frac{V_o(V_{in}-V_o)}{V_{in}Lf_{sw}}
$$

对于固定输出电压，输入电压升高时，电感电流纹波通常增大。因此先使用最高输入电压 16 V 进行计算。

本案例希望在标称开关频率下，将电感电流纹波控制在最大输出电流的约 30%，即：

$$
\Delta I_L\approx 2\times30\%=0.6\text{ A}
$$

使用 $V_{in}=16\text{ V}$、$V_o=5\text{ V}$、$f_{sw}=400\text{ kHz}$，可得：

$$
L
=
\frac{5(16-5)}{16\times400\text{ kHz}\times0.6}
\approx14.3\ \mu\text{H}
$$

选择邻近的标准感量 15 μH。在标称 400 kHz 下，最高输入电压时的纹波为：

$$
\Delta I_L
=
\frac{5(16-5)}{16\times15\ \mu\text{H}\times400\text{ kHz}}
\approx0.57\text{ A}
$$

约为最大输出电流的 29%，与最初的 30% 目标接近。

但是，芯片的实际开关频率存在误差。TPS54302 数据手册给出的开关频率范围约为 290～510 kHz。在其他条件不变时，较低开关频率会产生更大的电感电流纹波。使用 290 kHz 进行最差情况校核：

$$
\Delta I_{L(max)}
=
\frac{5(16-5)}{16\times15\ \mu\text{H}\times290\text{ kHz}}
\approx0.79\text{ A}
$$

此时电感峰值电流约为：

$$
I_{L(pk)}
=I_{o(max)}+\frac{\Delta I_{L(max)}}{2}
\approx2+\frac{0.79}{2}
\approx2.40\text{ A}
$$

以上结果仍然使用了 15 μH 的标称感量。假设所选电感的容差为 $\pm20\%$，最低感量可能下降至 12 μH。在最低开关频率、最高输入电压和最低感量同时出现时：

$$
\Delta I_{L(worst)}
=
\frac{5(16-5)}{16\times12\ \mu\text{H}\times290\text{ kHz}}
\approx0.99\text{ A}
$$

对应的最差峰值电流约为：

$$
I_{L(pk,worst)}
\approx2+\frac{0.99}{2}
\approx2.50\text{ A}
$$

电感有效值电流近似为：

$$
I_{L(rms)}
=
\sqrt{I_o^2+\frac{\Delta I_L^2}{12}}
\approx2.02\text{ A}
$$

因此，不能选择额定电流刚好等于 2 A 的电感。本案例可采用以下条件作为电感选型起点：

- 标称感量：15 μH；
- 饱和电流：不低于 3.5 A；
- 温升电流：不低于 2.5 A；
- 直流电阻：在尺寸允许的条件下尽可能低；
- 感量容差和高温下降后仍应具有足够余量。

饱和电流和温升电流的定义并不相同。饱和电流通常表示感量下降到规定比例时的电流，温升电流则表示电感温度升高到规定值时的电流。不同厂商使用的判定条件可能不同，选型时应阅读具体电感的数据手册。

15 μH 是根据本案例的纹波目标得到的初始值，而 TPS54302 数据手册的 5 V 典型应用使用 10 μH。数据手册也允许根据纹波要求调整电感，但由于芯片采用内部补偿，最终仍应结合输出电容、负载瞬态和实物环路测试确认稳定性，不能只依据纹波公式作出结论。

## 7.2.6 输入与输出电容的选择

### 7.2.6.1 输入电容

Buck 变换器的输入电流不是平滑的直流。高侧 MOSFET 导通时，输入端向电感和负载供电；高侧 MOSFET 关断时，输入电流迅速下降。因此，输入电容需要在芯片附近提供脉动电流，减小输入电源线上的高频电流和输入电压纹波。

输入电容承受的最差有效值纹波电流可以近似为：

$$
I_{Cin(rms)}
=I_o\sqrt{D(1-D)}
$$

当 $D=0.5$ 时，该式取得最大值：

$$
I_{Cin(rms,max)}\approx\frac{I_o}{2}=1\text{ A}
$$

因此，输入电容除了容量和耐压满足要求，还要能够承受相应的纹波电流。

TPS54302 数据手册建议使用不小于 10 μF 的陶瓷去耦电容。本案例输入最高为 16 V，可以选择两只 10 μF、25 V 的 X7R 陶瓷电容并联，并增加一只 0.1 μF 高频陶瓷电容。

两只 10 μF 并联不代表工作时一定具有 20 μF。多层陶瓷电容在直流偏压下可能出现明显的容量下降，封装越小、额定耐压越接近实际电压，这个问题通常越明显。选型时应查看厂商给出的直流偏压曲线，并保证在 16 V 和最高温度下的总有效容量仍不低于芯片所需的最低去耦容量。

如果输入电源距离变换器较远，或者输入线路阻抗较大，还可能需要额外的电解电容或固态电容承担较低频率的储能。但是，大容量电容不能代替靠近 VIN 与 GND 引脚的小型陶瓷电容。

### 7.2.6.2 输出电容

输出电容在开关周期内吸收电感纹波电流，并在负载突然增加、控制环路尚未来得及响应时暂时向负载提供能量。

忽略寄生参数时，由电容量造成的输出纹波近似为：

$$
\Delta V_{o(C)}
\approx
\frac{\Delta I_L}{8f_{sw}C_o}
$$

电容 ESR 产生的纹波近似为：

$$
\Delta V_{o(ESR)}
\approx
\Delta I_L\times ESR
$$

因此，输出纹波可以粗略写为：

$$
\Delta V_o
\approx
\frac{\Delta I_L}{8f_{sw}C_o}
+
\Delta I_L\times ESR
$$

本案例选择两只 22 μF 陶瓷电容并联，标称总容量为 44 μF。使用最低开关频率 290 kHz 和最大电感纹波 0.79 A，只考虑电容量时：

$$
\Delta V_{o(C)}
\approx
\frac{0.79}{8\times290\text{ kHz}\times44\ \mu\text{F}}
\approx7.7\text{ mV}
$$

该数值低于 50 mV 的稳态纹波目标。但是，这只是使用标称容量得到的理想结果。若考虑直流偏压降容、ESR、ESL 和 Layout 寄生参数，实际纹波会更大。

TPS54302 数据手册的 5 V 参考设计采用两只 22 μF、25 V、X7R 陶瓷电容。本案例沿用这一组标称参数，并通过具体电容的直流偏压曲线确认在 5 V 工作电压下的总有效容量。为了给内部补偿和负载瞬态保留余量，建议实际总有效容量不低于约 30 μF。

输出电容承受的有效值纹波电流近似为：

$$
I_{Cout(rms)}
\approx
\frac{\Delta I_L}{2\sqrt{3}}
$$

代入最大纹波 0.79 A，可得：

$$
I_{Cout(rms)}\approx0.23\text{ A}
$$

两只并联电容可以分担这部分纹波电流。

稳态纹波满足要求，并不代表负载瞬态一定满足要求。负载从轻载突然变化到 2 A 时，输出电容需要在控制器提高电感电流前暂时供电。由于本案例没有规定负载变化速度和允许的瞬态电压偏差，本节不对瞬态电容作唯一数值结论。实际设计应根据负载阶跃测试继续调整输出电容。

### 7.2.6.3 内部补偿对电感和电容的限制

内部补偿将误差放大器周围的补偿元件集成到了芯片内部，可以简化原理图，但也意味着设计者不能根据任意电感和电容重新调整内部补偿参数。

TPS54302 的 5 V 典型应用使用 10 μH 电感、两只 22 μF 输出电容以及 75 pF 前馈电容。本案例为了将标称电感纹波控制在约 30%，将电感初步增大到 15 μH，同时保留 44 μF 标称输出电容和 75 pF 前馈电容。

这种调整仍需要经过以下验证：

- 启动过程中输出是否单调上升；
- 空载、轻载和满载时是否稳定；
- 输入电压变化时是否出现异常振荡；
- 负载阶跃时输出是否出现持续振铃；
- 实际环路带宽和相位裕量是否满足要求。

本节不展开控制环路的零极点推导。更系统的反馈和稳定性问题将在第八章讨论。

## 7.2.7 汇总原理图并进行设计检查

完成以上计算后，可以将芯片和外围元件汇总到同一张原理图中。

本案例得到的初步元件参数如下。

| 元件 | 初步参数 | 主要校核内容 |
|---|---|---|
| U1 | TPS54302，SOT-23-THIN-6 | 输入范围、电流能力和散热 |
| L1 | 15 μH | 饱和电流不低于 3.5 A，温升电流不低于 2.5 A，低 DCR |
| CIN1、CIN2 | 10 μF、25 V、X7R，各一只 | 16 V 偏压下的总有效容量和纹波电流 |
| CIN_HF | 0.1 μF、25 V 或更高、X7R | 高频输入去耦 |
| COUT1、COUT2 | 22 μF、25 V、X7R，各一只 | 沿用数据手册参考设计，并确认 5 V 偏压下的总有效容量 |
| CBOOT | 0.1 μF、10 V 或更高、X7R/X5R | 连接在 BOOT 与 SW 之间 |
| $R_{FBT}$ | 100 kΩ | 1% 或更高精度 |
| $R_{FBB}$ | 13.5 kΩ | 1% 或更高精度，采购后重新反算输出值 |
| CFF | 75 pF、C0G/NP0 | 并联在上方反馈电阻两端 |
| EN | 悬空并加未连接标记 | 使用内部上拉默认使能，不连接 VIN |

![[tps54302_schematic.png]]
*图7-1 一种可供参考的原理图设计*

汇总原理图后，还应进行一次整体检查。

### 输入与输出范围检查

9～16 V 输入处于芯片 4.5～28 V 推荐工作范围内，5 V 输出低于最小输入电压，满足降压变换的基本条件。

最高输入电压与推荐工作上限之间存在余量，但是该余量不代表能够承受任意浪涌。如果输入线上可能出现超过 28 V 的尖峰，应单独评估输入保护。

### 电感与限流余量检查

按照最低开关频率估算，并进一步考虑电感 $-20\%$ 的感量容差，最大电感峰值电流约为 2.50 A。TPS54302 高侧峰值限流阈值的最小值约为 4 A，低侧谷值限流阈值的最小值约为 3.1 A，因此正常工作电流与芯片限流阈值之间具有一定余量。

限流阈值不能当作正常输出能力使用。芯片进入限流前，封装温升、电感温升或输入电压下降都可能已经成为实际限制。

### 电容检查

输入电容耐压高于 16 V，输出电容耐压高于 5 V。除了标称耐压，还需要检查：

- 陶瓷电容在直流偏压下的有效容量；
- 温度和老化造成的容量变化；
- 输入电容的纹波电流能力；
- 输出电容与内部补偿的配合；
- 实际器件封装是否满足 Layout 和散热要求。

### 输出精度检查

反馈分压的理论设定值约为 5.01 V，但芯片内部参考电压本身存在容差，反馈电阻也存在误差。因此，不能将 5.01 V 理解为实际电路一定输出 5.01 V。若负载对电压精度要求较高，应进行最差误差计算并根据实测结果调整电阻。

### 基本热检查

芯片的损耗主要包括内部上下 MOSFET 的导通损耗、开关损耗和控制电路损耗。由于 SOT-23-THIN 封装本身较小，PCB 铜皮是重要的散热路径。

原理图阶段可以根据效率估算总损耗，但最终结温不能只使用数据手册中单一的结到环境热阻机械计算。铜皮面积、层数、过孔、环境风速和周围热源都会改变实际温升。因此，满载温升必须在完成 Layout 后通过样机测试确认。

> 原理图连接正确只表示器件之间的电气关系正确，并不表示开关电流已经获得了合理的实际路径。

## 7.2.8 TPS54302 自带的保护功能

TPS54302 内部集成了多种保护功能。本案例不需要为这些内部保护配置外部 eFuse，也不需要使用外部电阻或电容设置其保护阈值。

### 输入欠压锁定

芯片内部具有固定的 VIN 欠压锁定功能。当输入电压低于内部阈值时，芯片停止开关；输入电压恢复后，芯片重新进入启动过程。本案例不使用 EN 分压设置更高的欠压启动门槛。

### 逐周期过流与打嗝保护

芯片会检测高侧和低侧 MOSFET 的电流。发生过流时，控制器可以限制当前开关周期或阻止下一次高侧导通。

如果过载持续一定时间，芯片会停止开关并等待，然后尝试重新启动，这种方式称为打嗝保护。它可以降低输出短路时的平均输入电流和芯片平均功耗。

### 输出过压保护

当 FB 引脚检测到输出电压明显高于设定值时，芯片会关闭高侧 MOSFET，待输出电压下降后再恢复。这一功能主要用于减小故障恢复或负载突然移除时的输出过冲。

该功能不能代替独立的精密输出过压切断电路，也不能保证在所有外部故障下保护负载。

### 过温关断

当芯片结温过高时，内部热保护会停止开关。温度降低并经过内部等待过程后，芯片可以尝试重新启动。

过温关断属于故障状态下的最后保护，不能作为正常的温度控制方式。如果样机在额定负载下反复进入过温保护，应重新检查损耗、PCB 散热和负载条件。

### 内部软启动

TPS54302 还具有约 5 ms 的内部软启动过程，可以限制启动时输出电压的上升速度，减小启动浪涌和输出过冲。软启动时间由芯片内部设定，本案例不需要外接软启动电容。

以上功能主要用于保护变换器芯片和内部功率级，并不等同于完整的系统级输入保护。TPS54302 不能单独代替输入反接保护、浪涌箝位、线路保险和接口 ESD 保护。是否需要这些外部电路，应根据实际输入电源和使用环境另行判断。

完成元件计算和原理图连接，只能说明电路在原理上具备工作的条件。开关电流实际流经的路径、元件之间的距离、地平面的处理以及散热铜皮，仍然会直接影响电路的纹波、稳定性、EMI 和可靠性。下一节将进一步讨论集成 Buck 变换器的 PCB Layout。

# 7.3 集成 Buck 变换器的 PCB Layout

完成原理图后，下一步是将电路中的电气连接转化为 PCB 上真实的元件、铜皮和过孔。

对于低频或静态信号，只要连接关系正确，较小的走线差异可能不会明显改变电路行为。但是，Buck 变换器中存在快速变化的电压和电流。走线和铜皮会产生寄生电阻、寄生电感和寄生电容，因此原理图中看似相同的连接，在不同 Layout 下可能得到完全不同的纹波、EMI、温升和稳定性表现。

当核心芯片的数据手册中提供推荐 Layout 时，应当首先参考厂商给出的布局示例和布局规则。芯片厂商更了解芯片内部功率开关、引脚排列、散热路径和控制电路的特点，推荐 Layout 通常比脱离具体器件的通用经验更具有直接参考价值。

不过，推荐 Layout 也不能代替设计者对电流路径的理解。当 PCB 外形、接口位置、层数或者外围元件发生变化时，往往无法完全复制数据手册中的示例。只有理解各项规则的原因，才能在布局条件改变后作出合理调整。

Layout 是一项需要综合考虑电气、热、机械、制造和成本的工程。本节无法完整覆盖 PCB 设计中的所有问题，只针对前文 TPS54302 Buck 案例，介绍其中较为重要的高频回路、反馈、电感、去耦、接地和散热问题。

## 7.3.1 识别关键回路

进行元件布局之前，首先需要识别电路中哪些路径承载连续电流，哪些回路中的电流会快速切换，以及哪些节点的电压会快速变化。

如果没有先区分这些路径，只按照原理图从左到右摆放元件，虽然网络连接可能全部正确，实际 PCB 仍然可能出现输入振铃、输出纹波过大、反馈误动作和 EMI 超标等问题。

### 7.3.1.1 高频输入开关回路

TPS54302 内部集成了高侧和低侧 MOSFET。高侧 MOSFET 导通时，输入电容通过 VIN、高侧 MOSFET、SW 和电感向输出侧传递能量；高侧 MOSFET 关断后，电感电流转移到低侧 MOSFET 中续流。

在两个状态切换的瞬间，输入电流会快速变化。与这种换流过程直接相关的高频回路主要由以下部分组成：

输入陶瓷电容正端
→ VIN 引脚
→ 芯片内部半桥换流单元
→ GND 引脚
→ 输入陶瓷电容负端

这里的半桥换流单元由高侧与低侧 MOSFET 交替工作形成，并不表示两个 MOSFET 同时导通。这个回路中的电流具有较高的 $\mathrm{d}i/\mathrm{d}t$。回路面积越大，寄生电感通常越大，开关瞬间产生的电压尖峰和电磁辐射也越明显。因此，输入陶瓷电容不能只在网络上连接 VIN 和 GND，还必须在物理位置上靠近 TPS54302 的 VIN 与 GND 引脚。

### 7.3.1.2 输出功率路径

SW 节点经过电感连接到输出电容和负载。与输入高频回路相比，电感会限制输出侧电流的变化速度，因此输出电流相对连续。但是，输出电容仍然需要承担电感纹波电流，并在负载变化时暂时提供或吸收电流。

输出功率路径应当短而宽，使电感、输出电容和负载之间的寄生阻抗尽可能小。这里所说的“短”不是要求所有元件挤在同一点，而是要求大电流路径明确，避免绕行、突然变窄和使用不必要的过孔。

### 7.3.1.3 SW 节点

SW 节点连接内部高侧 MOSFET、低侧 MOSFET、输出电感和 Bootstrap 电容。其电压会在接近输入电压和接近地电位之间快速切换，是整个 Buck 电路中 $\mathrm{d}v/\mathrm{d}t$ 较高的节点。

SW 铜皮需要足够宽，以承载电流并降低连接阻抗；但其面积不应无目的地扩大。较大的 SW 铜皮会通过寄生电容向地平面和周围走线耦合更多开关噪声，也可能形成更强的辐射源。

因此，SW 节点的基本原则是：

- 从芯片 SW 引脚到电感的连接尽量短；
- 铜皮宽度满足载流和散热要求；
- 不为了外观整齐而将 SW 铜皮延伸到无关区域；
- 远离 FB、时钟、采样和通信等敏感信号；
- 不在其下方或附近布置没有必要的敏感铜皮和走线；
- 具体铜皮形状优先参考芯片数据手册的推荐 Layout。

### 7.3.1.4 FB 反馈路径

FB 引脚只检测约 0.596 V 的反馈电压，是一个对噪声较敏感的小信号节点。如果 FB 走线靠近 SW 节点或电感，开关噪声可能通过寄生电容和互感耦合到反馈回路，使控制器误判输出电压。

反馈分压的上方电阻应使用一条独立采样路径连接到输出电容正端附近，而不是从电感与输出铜皮之间任意取点。下方反馈电阻的地端应连接到安静的参考地，避免与输入电容、低侧 MOSFET 等高频脉动回流共用一段具有明显阻抗的铜皮。

这种做法可以称为 Kelvin 采样。其目的不是改变原理图网络名称，而是避免大电流在铜皮上产生的压降被反馈网络一并测量。

FB 走线还应满足：

- 尽量短，并减小 FB 节点暴露的铜皮面积；
- 远离 SW 节点和电感；
- 不与高电流走线长距离平行；
- 不跨越地平面缝隙；
- 必要时使用安静的地铜进行屏蔽，但不能因此切断其他高频回流路径。

## 7.3.2 注意电感元件

电感是 Buck 输出功率路径中的主要元件，同时也是体积较大、具有磁场并且会发热的元件。电感的位置不仅影响功率走线长度，还会影响周围小信号的噪声和整块 PCB 的温度分布。

电感应靠近 TPS54302 的 SW 引脚，使 SW 到电感之间的高噪声连接尽可能短。但是，电感靠近芯片不等于焊盘必须与芯片引脚紧贴。仍需满足焊接、返修和散热所需的间距，同时避免为了连接较大的电感焊盘而将 SW 铜皮扩展到过大的区域。

电感工作时，绕组中的纹波电流会产生交变磁场。非屏蔽电感的漏磁通常较强，更容易在周围导体中感应噪声；屏蔽电感可以减小外部漏磁，但也不代表其周围可以任意布置敏感信号。

对于电感下方是否铺铜，不能给出适用于所有器件的统一答案。

- 如果电感厂商规定了禁布区或禁止铺铜区域，应当优先遵守器件数据手册；
- 非屏蔽电感下方的铜皮可能受到较强磁场影响，产生涡流和附加损耗，此时通常需要保留适当的避空区域；
- 部分屏蔽电感允许下方保留连续地铜，以提供一定屏蔽或散热作用；
- 即使允许铺地，也不应让 FB 等敏感信号从电感下方穿过；
- SW 铜皮不应借助电感下方区域无目的地扩大。

走线方向也会影响磁耦合。在条件允许时，使重要信号走线方向与主要漏磁方向尽量正交，可以减小部分耦合。但是，方向调整只能作为辅助手段，不能代替增加距离、减小信号回路面积和选用屏蔽电感。

电感两端承载完整输出电流，其焊盘和连接铜皮应具有足够宽度。走线突然变窄、使用过少的过孔或在焊盘出口处形成细颈，都会增加铜损和局部温升。对于体积较大的电感，还应考虑焊盘的机械强度和可制造性。

在 TPS54302 案例中，15 μH 电感应靠近 SW 引脚放置，电感另一端直接进入 5 V 输出铜皮和输出电容区域。反馈分压网络应放在电感输出侧的安静区域，而不是放在 SW 端或电感正下方。

## 7.3.3 去耦电容的靠近问题

在原理图中，只要电容两端分别连接到指定网络，电气连接就是相同的。但是在高频条件下，电容焊盘、走线和过孔都具有寄生电感。电容与芯片之间的连接越长，电容在开关瞬间能够发挥的去耦作用通常越差。

因此，“去耦电容靠近芯片”更准确的含义是：电容、芯片引脚和回流路径共同形成的回路面积应尽可能小，而不只是电容外形与芯片外形之间的直线距离较短。

### 输入电容

TPS54302 的输入陶瓷电容应优先放置在 VIN 与 GND 引脚附近。VIN 到电容正端、GND 到电容负端的连接都要短，不能只让正端靠近 VIN，却让负端经过很长的地线才回到 GND。

本案例使用两只 10 μF 输入陶瓷电容和一只 0.1 μF 高频电容。0.1 μF 电容可以靠近引脚，用于旁路更高频率的边沿分量；10 μF 电容则需要承担主要的开关脉动电流，也必须处于短小的输入高频回路中。

多只并联电容不应简单排成很长的一列，使距离芯片较远的电容通过细长走线连接到 VIN。应尽量使用低阻抗铜皮，让每只电容都能够有效连接到输入与地。其中最靠近芯片的陶瓷电容及其回流路径最为关键。

### Bootstrap 电容

0.1 μF Bootstrap 电容连接在 BOOT 与 SW 之间，为内部高侧 MOSFET 的驱动提供能量。该回路电流脉冲较快，因此电容应直接靠近 BOOT 和 SW 引脚，避免使用长走线或跨层绕行。

需要再次注意，Bootstrap 电容的参考端是 SW，而不是 GND。为了追求“靠近地”而将其错误连接到地，会使高侧驱动无法正常工作。

### 输出电容

输出电容应靠近电感输出端，并通过低阻抗路径连接到功率回流。输出电容与芯片的物理距离通常不如输入电容与芯片的距离敏感，但是电感、输出电容和 GND 之间仍不应形成较大的回路。

反馈采样点应位于输出电容附近，使反馈网络检测到经过输出电容滤波后的实际输出电压。若输出接口距离电源较远，还需要区分是调节变换器本地输出，还是补偿连接器与长走线上的压降。后者涉及远端采样能力，不能在普通 FB 引脚上随意实现。

### 走线与过孔

去耦电容使用过孔连接到其他层时，过孔本身也会增加寄生电感。应将过孔靠近电容焊盘放置，必要时使用多个并联过孔降低阻抗。不能先从电容焊盘引出一段细长走线，再在远处放置过孔。

增加过孔数量并不一定能够弥补不合理的元件位置。布局时应先缩短高频回路，再使用合适的走线宽度和过孔改善层间连接。

## 7.3.4 地与回流路径

“地”在原理图中通常用同一个符号表示，但是 PCB 上的地由具有实际电阻和电感的铜皮组成。只要有电流流过，就可能在不同位置之间产生电位差。

低频电流主要倾向于沿电阻较小的路径返回；频率升高后，寄生电感和回路面积的影响变得更加明显。高频回流通常会尽量靠近正向电流路径，以减小磁场和总回路电感。因此，不能只根据几何距离判断回流电流会从哪里经过。

### 使用连续地平面

连续地平面能够为不同信号提供较低阻抗的回流路径，也能减小回流被迫绕行的可能。对于许多 Buck PCB，保持较完整的地平面，再通过合理的元件位置控制不同电流的流动区域，是比较稳妥的方法。

采用这种方法时，不需要在 PCB 文件中将地划分成多个不同网络，但仍需注意：

- 输入电容的高频回流应直接返回芯片 GND；
- 输出电容的回流应通过宽铜皮连接到功率地；
- FB 分压电阻的地端应位于安静区域；
- 高电流路径不应穿过 FB 网络附近；
- 不应使用无意义的开槽破坏高频回流的连续性。

连续地平面不等于可以忽略电流方向。即使所有位置都是 GND，只要布局不合理，大电流仍可能经过敏感电路下方并产生公共阻抗耦合。

### 信号地与功率地分区后单点连接

另一种方法是区分信号地和功率地，再在预定位置进行单点连接。

功率地主要承担输入电容、低侧 MOSFET、输出电容和负载的电流；信号地主要作为反馈分压和控制电路的参考。合理分区可以防止大电流压降直接叠加到敏感信号地上。

但是，分区并不等同于随意切割地平面。如果连接点位置错误，或者重要信号跨过两个地之间的缝隙，高频回流会被迫绕过开槽，形成更大的回路。分区方法只有在能够明确正向路径、回流路径和单点连接位置时才适合使用。

对于 TPS54302，芯片只有一个 GND 引脚，同时承担内部控制地和低侧功率 MOSFET 的回流。因此，无论采用连续地平面还是分区方法，都不能在芯片 GND 引脚附近制造狭窄、曲折的连接。FB 下方电阻的地端应通过独立、安静的路径连接到 GND 参考点，不应与输入电容的脉动回流串联共用一段细铜皮。

TPS54302 数据手册还要求敏感反馈地与 GND 进行单点连接，并避免开关电流从芯片下方经过。这并不是要求芯片下方完全没有地铜，而是要求外部高频功率回流不要借芯片下方区域绕行并干扰控制部分。

### 地平面缝隙与过孔

一条信号从某层经过时，其高频回流通常需要在相邻参考平面上跟随。如果信号跨过地平面的开槽、分割边界或大面积避空区，回流就需要绕行，导致回路面积增大。

因此，FB、时钟和通信等重要信号不应跨越地平面缝隙。信号换层时，如果参考平面也发生变化，应在换层位置附近提供合适的地过孔或其他连续回流路径。

地过孔可以降低不同地层之间的连接阻抗，也可以参与散热。过孔应放在实际电流需要换层的位置附近，而不是只为了数量均匀分布在板上。对于输入和输出电容，地过孔应靠近电容负端焊盘；对于高频回路，多个并联过孔通常比单个远距离过孔更有效。

## 7.3.5 散热问题

变换器中的电能损耗最终会转化为热。TPS54302 内部高侧和低侧 MOSFET 会产生导通损耗和开关损耗，电感会产生绕组铜损和磁芯损耗，电容的 ESR 也会产生一定损耗。

TPS54302 使用较小的 SOT-23-THIN 封装，芯片产生的热量需要通过引脚、封装表面和 PCB 铜皮向外扩散。数据手册中的结到环境热阻可以用于初步比较，但是实际热阻会受到铜皮面积、PCB 层数、铜厚、过孔、风速、板框尺寸和周围热源的明显影响。

PCB 上可以采用以下方式改善散热：

- 在不破坏高频回路的前提下，为载流和散热引脚提供足够的铜皮；
- 使用连续地平面帮助热量向较大区域扩散；
- 在需要层间导热的位置设置多个热过孔；
- 避免在大电流路径上使用细颈和过少的过孔；
- 避免芯片、电感和其他发热器件过度集中；
- 为自然对流和外壳导热保留合理条件。

散热铜皮并不是越大越好。特别是 SW 节点，一方面需要较低的电阻并可能承担部分热扩散，另一方面又是高 $\mathrm{d}v/\mathrm{d}t$ 噪声节点。无目的地扩大 SW 铜皮可能降低一点温升，却增加寄生电容和 EMI。实际形状应优先参考 TPS54302 的推荐 Layout，在散热和噪声之间取得平衡。

两层板通常主要依靠顶层和底层铜皮扩散热量，并通过地过孔将两面连接；多层板可以利用内层平面获得更大的热扩散面积。不过，层数本身不能保证散热良好。如果器件焊盘与内层之间缺少有效的铜连接，增加内层也不会明显降低温升。

电感本身也可能是主要热源。较高的 DCR 会增加绕组损耗，磁芯损耗则与开关频率、纹波电流和磁性材料有关。电感与芯片距离过近时，两者可能互相加热。因此，在保持 SW 连接较短的同时，也要避免两个高温器件之间完全没有散热空间。

温度升高还会进一步改变器件参数：

- MOSFET 导通电阻通常随温度升高而增大；
- 电感的饱和能力和允许温升可能下降；
- 电容的有效容量、ESR 和寿命会发生变化；
- 芯片能够可靠输出的实际电流可能降低。

因此，过温关断不能作为正常的热设计目标。完成 PCB 后，应在最低和最高输入电压、空载和满载、规定环境温度等条件下测量芯片、电感和输入输出电容的温度。必要时还应检查封闭外壳内的温升，而不能只在开放桌面环境下测量。

完成 Layout 后，可以使用以下项目进行初步检查：

- 是否参考了 TPS54302 数据手册的推荐 Layout；
- 输入陶瓷电容是否真正靠近 VIN 与 GND，并形成最小高频回路；
- Bootstrap 电容是否靠近 BOOT 与 SW；
- SW 到电感的连接是否短而明确，SW 铜皮是否被无目的地扩大；
- 输出电容是否靠近电感输出端和功率回流；
- FB 是否从输出电容附近独立采样，并远离 SW 和电感；
- 反馈地是否连接到安静参考点；
- 重要信号是否跨越地平面缝隙；
- 大电流走线是否具有足够宽度和过孔数量；
- 地平面是否为高频电流提供了连续回流路径；
- 芯片和电感是否具有合理的散热铜皮与间距；
- 测试点是否便于测量，同时没有明显扩大高噪声节点。

![[tps54302_layout.png]]
*图7-2 一种可供参考的layout设计*

推荐 Layout、原理分析和样机测试三者不能互相代替。推荐 Layout 提供经过验证的起点，原理分析帮助设计者适应不同的板框和接口限制，样机测试则用于确认纹波、开关波形、EMI、环路响应和温升是否真正满足要求。

因此，PCB 绘制完成并不代表设计已经结束。正式投入使用之前，还需要制定合理的上电顺序，在限流条件下完成首次上电，并通过正确的测量方法检查输入、输出、SW 节点和关键器件温度。
