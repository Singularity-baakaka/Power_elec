# 开关器件：从理想开关到功率半导体器件

在前面的讨论中，我们把开关抽象成了一个只有“导通”和“关断”两种状态的理想元件。

利用开关的周期性动作，我们可以改变电路中的能量传递路径；再借助电感和电容的储能作用，就可以通过占空比控制变换器的平均输出。

但是，真实的开关器件与理想开关之间存在明显差别。

真实的功率开关不能凭空导通或者关断。它需要驱动电路提供合适的控制电压或控制电流，而且在导通、关断以及状态转换的过程中都会产生损耗。

因此，在开始研究具体变换器之前，我们需要对实际使用的功率开关器件建立一个基本认识。

本章主要介绍两类常见的功率半导体器件：

1. 功率 MOSFET
2. IGBT

我们将重点讨论：

- 它们通过什么信号控制；
- 怎样使其导通和关断；
- 导通状态和关断状态有什么实际特性；
- 为什么需要专门的驱动器；
- 器件的寄生电容会带来什么影响；
- 开关器件的主要损耗来自哪里。

至于 MOSFET 和 IGBT 内部更加详细的半导体结构、载流子运动过程以及完整输出特性推导，可以结合模拟电子技术和半导体器件教材进一步学习。


---

## 1. 理想开关

理想开关具有导通和关断两种状态。

### 1.1 理想导通状态

理想开关导通时，相当于一根没有电阻的导线，其导通电阻为

$$
R_{\mathrm{on}}=0
$$

因此，理想开关两端的电压为

$$
v_{\mathrm{sw}}=0
$$

即使此时有较大的电流流过开关，其功率损耗仍然为

$$
p_{\mathrm{sw}}
=
v_{\mathrm{sw}}i_{\mathrm{sw}}
=
0
$$

### 1.2 理想关断状态

理想开关关断时，相当于一个完全断开的支路，其关断电阻为

$$
R_{\mathrm{off}}\rightarrow\infty
$$

因此，流过理想开关的电流为

$$
i_{\mathrm{sw}}=0
$$

即使此时开关两端承受较高的电压，其功率损耗仍然为

$$
p_{\mathrm{sw}}
=
v_{\mathrm{sw}}i_{\mathrm{sw}}
=
0
$$

### 1.3 理想切换过程

理想开关还具有以下假设：

- 导通和关断可以在一瞬间完成；
- 导通时没有电压降；
- 关断时没有漏电流；
- 控制开关不需要消耗能量；
- 开关可以承受任意大的电压和电流；
- 开关状态不会受到温度和寄生参数影响。

显然，实际器件不可能完全满足这些条件。

理想开关只是为了帮助我们首先理解变换器的拓扑结构、开关状态和能量路径。在完成理想分析以后，还需要逐步把真实器件的损耗、寄生参数和额定限制加入模型。

---

## 2. 真实开关与理想开关的区别

真实的功率半导体器件至少存在以下几方面的非理想特性。

### 2.1 导通时存在损耗

真实开关导通后，其两端电压不会严格等于零。

MOSFET 在导通状态下通常表现出一定的导通电阻 $R_{\mathrm{DS(on)}}$。如果流过 MOSFET 的电流为 $i_D$，那么其导通电压近似为

$$
v_{\mathrm{DS}}
\approx
i_D R_{\mathrm{DS(on)}}
$$

相应的瞬时导通损耗近似为

$$
p_{\mathrm{cond}}
\approx
i_D^2R_{\mathrm{DS(on)}}
$$

IGBT 导通时则通常表现为一定的集电极—发射极饱和压降 $V_{\mathrm{CE(sat)}}$，其导通损耗可以近似表示为

$$
p_{\mathrm{cond}}
\approx
V_{\mathrm{CE(sat)}}i_C
$$

因此，真实开关即使处于稳定导通状态，也会产生功率损耗和温升。

### 2.2 关断时存在漏电流

真实器件关断以后，阻断电阻并不是无穷大，仍然可能存在较小的漏电流。

### 2.3 导通和关断需要时间

真实开关不能从关断状态瞬间变为完全导通，也不能从导通状态瞬间变为完全关断。

在开通和关断过程中，器件两端可能同时存在较大的电压和电流。此时，器件的瞬时功率为

$$
p_{\mathrm{sw}}(t)
=
v_{\mathrm{sw}}(t)i_{\mathrm{sw}}(t)
$$

当电压和电流在切换过程中发生重叠时，就会产生开关损耗。对上式积分（即重叠面积），就是损耗的功。

如果器件每次开通和关断所消耗的能量分别为 $E_{\mathrm{on}}$ 和 $E_{\mathrm{off}}$，开关频率为 $f_s$，则平均开关损耗可以粗略表示为

$$
P_{\mathrm{sw}}
\approx
\left(E_{\mathrm{on}}+E_{\mathrm{off}}\right)f_s
$$

这说明，提高开关频率虽然能够减小电感电流和输出电压的纹波，却也可能增加开关损耗。

### 2.4 开关需要驱动器

真实的 MOSFET 和 IGBT 都具有控制端。

控制器产生的 PWM 信号通常不能直接驱动大功率器件。这是因为数字控制器的输出端：

- 输出电压不一定满足功率器件的要求；
- 瞬时输出电流能力有限；
- 不能快速完成栅极电荷的充电和放电；
- 可能无法承受功率回路产生的干扰；
- 不能直接驱动电位不断变化的高侧开关；
- 缺少过流、欠压、短路和隔离保护。

因此，在控制器与功率开关之间，通常需要加入专门的栅极驱动器。

### 2.5 开关具有额定限制

真实器件能够承受的电压、电流和功率都是有限的。

选用器件时至少需要考虑：

- 最大漏源电压或集电极—发射极电压；
- 连续电流和脉冲电流；
- 最大允许结温；
- 最大功率耗散；
- 短路承受能力；
- 栅极允许电压；
- 封装和散热条件。

---

## 3. 功率 MOSFET

MOSFET 是开关电源中最常见的功率开关器件之一。

MOSFET 的三个主要端子是：

- 栅极 Gate，记为 $G$；
- 漏极 Drain，记为 $D$；
- 源极 Source，记为 $S$。

在功率变换器中，N 沟道 MOSFET 的使用最为普遍，因此本章主要以 N 沟道功率 MOSFET 为例。

控制 MOSFET 的是栅极与源极之间的电压：

$$
V_{\mathrm{GS}}
=
V_G-V_S
$$

这一点非常重要。

如果 MOSFET 的源极电位发生变化，那么即使栅极对地电压保持不变，真正决定 MOSFET 工作状态的 $V_{\mathrm{GS}}$ 仍然可能发生变化。

因此，在分析或测量 MOSFET 的驱动波形时，必须关注真实的栅源电压 $V_{\mathrm{GS}}$。

---

## 4. MOSFET 的基本控制方式

MOSFET 是一种由栅源电压控制的器件。

从直流稳态角度看，MOSFET 的栅极与沟道之间存在绝缘层，理想情况下不会有持续的栅极电流。因此，人们常把 MOSFET 称为“电压控制型器件”。

但是，“电压控制型器件”并不意味着驱动 MOSFET 时完全不需要电流。

真实 MOSFET 的栅极与其他端子之间存在寄生电容。每次开通时，驱动器都需要向这些电容充电；每次关断时，驱动器又需要把其中的电荷释放掉。

因此，更准确的说法是：

> MOSFET 的稳态导通状态主要由栅源电压决定，但它的动态开通和关断速度取决于驱动器对栅极电荷的充放电能力。

### 4.1 MOSFET 关断

当栅源电压较低时，MOSFET 沟道没有充分形成，器件处于关断状态。

此时，漏极与源极之间近似没有电流流过，但器件可能承受较高的漏源电压

$$
V_{\mathrm{DS}}
=
V_D-V_S
$$

### 4.2 MOSFET 导通

当栅源电足够高时，MOSFET 沟道形成，漏极与源极之间可以流过电流。

在充分导通并进入低阻状态后，可以近似写成

$$
V_{\mathrm{DS}}
\approx
I_D R_{\mathrm{DS(on)}}
$$

需要注意，数据手册中的栅极阈值电压 $V_{\mathrm{GS(th)}}$ 只表示沟道刚刚开始形成。

在阈值电压附近，MOSFET 通常只能流过很小的测试电流，并不代表器件已经适合承载额定功率。

因此：

> $V_{\mathrm{GS(th)}}$ 不是推荐的完全导通驱动电压。

设计驱动电压时，应查阅数据手册中给出的 $R_{\mathrm{DS(on)}}$ 测试条件、栅极电荷和推荐驱动条件，而不能只根据阈值电压判断 MOSFET 是否已经完全导通。

---

## 5. MOSFET 的输入特性和输出特性

### 5.1 输入特性

从控制端看，MOSFET 的输入主要表现为一个需要充电和放电的非线性电容网络。

在稳定导通或稳定关断时，栅极通常不需要持续提供较大的直流电流；但是在开通和关断过程中，驱动器需要提供较大的瞬时电流，以便快速改变栅极电压。

如果 MOSFET 的总栅极电荷为 $Q_g$，驱动电压变化范围近似为 $V_{\mathrm{drv}}$，开关频率为 $f_s$，那么栅极驱动所需的平均功率可以粗略估计为

$$
P_{\mathrm{gate}}
\approx
Q_gV_{\mathrm{drv}}f_s
$$

这个表达式说明：

- 总栅极电荷越大，驱动器需要传递的电荷越多；
- 驱动电压越高，每个周期需要的驱动能量越大；
- 开关频率越高，栅极驱动功率越大。

因此，控制器输出端即使能够提供正确的逻辑电压，也不一定具有足够的瞬时电流快速驱动功率 MOSFET。

### 5.2 转移特性

MOSFET 的转移特性描述在一定漏源电压条件下，漏极电流 $I_D$ 与栅源电压 $V_{\mathrm{GS}}$ 之间的关系。

一般来说，当 $V_{\mathrm{GS}}$ 低于阈值电压附近时，MOSFET 基本关断；随着 $V_{\mathrm{GS}}$ 增大，沟道导电能力逐渐增强，允许流过的漏极电流增大。

但是，功率变换器通常不希望 MOSFET 长时间停留在中间的线性区域。

原因是此时 MOSFET 可能同时承受较大的 $V_{\mathrm{DS}}$ 和 $I_D$，产生较大的功率损耗：

$$
p_D
=
V_{\mathrm{DS}}I_D
$$

因此，在开关应用中，我们通常希望 MOSFET：

- 关断时足够彻底；
- 导通时进入足够低的导通电阻状态；
- 尽快经过中间的线性区域。

### 5.3 输出特性

MOSFET 的输出特性通常描述在不同 $V_{\mathrm{GS}}$ 条件下，漏极电流 $I_D$ 与漏源电压 $V_{\mathrm{DS}}$ 之间的关系。

当 MOSFET 被充分驱动，并工作在适合作为开关的低阻区域时，其输出特性可以近似理解为一个受 $V_{\mathrm{GS}}$ 影响的导通电阻。

但是，$R_{\mathrm{DS(on)}}$ 并不是一个始终不变的常数。它通常受到以下因素影响：

- 栅源驱动电压；
- 结温；
- 漏极电流；
- 器件材料和芯片结构；
- 器件所承受的额定电压等级。

通常情况下，结温升高会使 MOSFET 的导通电阻增大，从而增加导通损耗。

---

## 6. MOSFET 的寄生电容

MOSFET 内部存在多个寄生电容。

需要注意的是，这些电容并不都适合简单称为“结电容”。其中一部分与器件的 PN 结和耗尽区有关，另一部分则来自栅极绝缘层、器件结构以及端子之间的电场耦合。

分析中，通常把它们统称为 MOSFET 的管间电容或寄生电容。

主要包括：

- 栅源电容 $C_{\mathrm{GS}}$；
- 栅漏电容 $C_{\mathrm{GD}}$；
- 漏源电容 $C_{\mathrm{DS}}$。

### 6.1 栅源电容

栅源电容 $C_{\mathrm{GS}}$ 影响栅源电压建立和下降的速度。

当驱动器向栅极提供电流时，需要先对相关电容充电，$V_{\mathrm{GS}}$ 才能逐渐上升。关断时，驱动器则需要把栅极电荷释放出去，使 $V_{\mathrm{GS}}$ 下降。

因此，驱动器的输出电流能力和栅极回路阻抗会直接影响 MOSFET 的开关速度。

### 6.2 栅漏电容与 Miller 效应

栅漏电容 $C_{\mathrm{GD}}$ 连接在栅极和漏极之间。

由于 MOSFET 开关时漏源电压 $V_{\mathrm{DS}}$ 会快速变化，漏极电压的变化会通过 $C_{\mathrm{GD}}$ 耦合到栅极。

流过栅漏电容的电流可以近似表示为

$$
i_{\mathrm{GD}}
=
C_{\mathrm{GD}}
\frac{\mathrm{d}v_{\mathrm{GD}}}{\mathrm{d}t}
$$

因此，当漏极电压具有很大的变化率时，即使 $C_{\mathrm{GD}}$ 很小，也可能产生不可忽略的电流。

这种耦合与 Miller 效应密切相关。在 MOSFET 开通或关断过程中，栅极电压可能出现一段变化较慢的平台，这通常称为 Miller 平台。

在桥式电路中，另一个开关快速动作时产生的高 $\mathrm{d}v/\mathrm{d}t$ 还可能通过 $C_{\mathrm{GD}}$ 向本应关断的 MOSFET 栅极注入电流，使其栅源电压短暂升高。如果处理不当，可能造成误导通甚至桥臂直通。

### 6.3 漏源电容

漏源电容 $C_{\mathrm{DS}}$ 会影响漏源电压在开关过程中的变化。

在器件开通和关断时，漏源电容需要充电或放电。储存在其中的能量会参与换流过程，并可能形成额外损耗。

如果把输出电容粗略看成一个固定电容 $C_{\mathrm{oss}}$，其储能可近似写成

$$
E_{\mathrm{oss}}
\approx
\frac{1}{2}C_{\mathrm{oss}}V_{\mathrm{DS}}^2
$$



---

## 7. 数据手册中的电容参数

MOSFET 数据手册通常不会只给出 $C_{\mathrm{GS}}$、$C_{\mathrm{GD}}$ 和 $C_{\mathrm{DS}}$，而是给出以下三个组合参数：

### 输入电容

$$
C_{\mathrm{iss}}
=
C_{\mathrm{GS}}+C_{\mathrm{GD}}
$$

输入电容主要反映驱动器从栅极端看到的电容效应。

### 输出电容

$$
C_{\mathrm{oss}}
=
C_{\mathrm{DS}}+C_{\mathrm{GD}}
$$

输出电容主要影响漏源电压变化、换流过程和相关能量损耗。

### 反向传输电容

$$
C_{\mathrm{rss}}
=
C_{\mathrm{GD}}
$$

反向传输电容反映漏极电压变化向栅极耦合的程度，与 Miller 效应和高 $\mathrm{d}v/\mathrm{d}t$ 误导通密切相关。

这些电容通常是非线性的，会随着 $V_{\mathrm{DS}}$ 等工作条件变化。因此，在不同电压下，数据手册曲线中给出的电容值可能相差很大。

---

## 8. MOSFET 的体二极管

功率 MOSFET 的内部结构通常会形成一个体二极管。

对于常见的 N 沟道功率 MOSFET，这个二极管可以在特定方向上为反向电流提供通路。

在桥式变换器和同步整流电路中，体二极管可能在以下过程中导通：

- 两个开关之间的死区时间；
- 电感电流换流；
- 启动和关断过程；
- 故障过程；
- 同步开关未能及时导通的时间段。

但是，体二极管并不是一个理想二极管。它可能存在：

- 正向压降；
- 反向恢复；
- 结电容；
- 导通损耗；
- 反向恢复引起的电流尖峰和额外开关损耗。

特别是在硬开关电路中，体二极管的反向恢复特性可能明显影响效率、电压尖峰和电磁干扰。



---

## 9. 不同材料的功率场效应器件

功率开关器件可以使用不同的半导体材料和器件结构。

常见器件包括：

- 硅 MOSFET；
- 碳化硅 MOSFET；
- 氮化镓功率器件。

不同材料和结构在耐压、导通损耗、开关速度、寄生参数、温度能力、驱动要求和成本方面各有特点。

### 9.1 硅 MOSFET

硅 MOSFET 技术成熟、种类丰富、成本相对较低，在中低压开关电源、同步整流、电机驱动以及各种 DC-DC 变换器中应用广泛。

它的主要优点包括：

- 驱动和应用技术成熟；
- 器件选择丰富；
- 成本相对较低；
- 在合适的电压和功率范围内具有较低的导通损耗；
- 可以实现较高的开关频率。

但是，随着额定耐压提高，硅 MOSFET 的导通电阻和相关寄生参数可能明显增加。

### 9.2 碳化硅 MOSFET

碳化硅 MOSFET 通常用于较高电压、较高温度或者需要降低高压开关损耗的场合。

它通常具有以下特点：

- 较强的高压工作能力；
- 较高的允许结温；
- 较快的开关速度；
- 在高压条件下具有较好的导通性能；
- 适合提高高压变换器的开关频率和功率密度。

但是，碳化硅 MOSFET 的快速电压和电流变化也会对驱动和布局提出更高要求，例如：

- 栅极驱动回路需要足够紧凑；
- 需要控制寄生电感；
- 需要防止高 $\mathrm{d}v/\mathrm{d}t$ 引起误导通；
- 需要根据数据手册选择正确的驱动电压；
- 可能需要使用负压关断或 Miller 钳位；
- 需要更加重视电磁干扰。

### 9.3 氮化镓功率器件

常见的氮化镓功率器件通常采用 HEMT 等结构，因此严格来说，并不是传统意义上的硅功率 MOSFET。

但是，从变换器使用者的角度看，它同样属于由栅极控制的功率开关器件。

氮化镓器件通常具有较小的栅极电荷、较快的开关速度和较小的部分寄生参数，适合高频、高功率密度的应用。

与此同时，它的驱动电压范围、栅极耐压、封装寄生参数和布局要求通常更加严格。某些氮化镓器件也不能直接套用硅 MOSFET 的体二极管模型，需要根据具体器件的数据手册分析其反向导电特性。

因此，不能简单地根据材料判断一个器件一定适合某个开关频率。

器件是否合适，还需要综合考虑：

- 输入和输出电压；
- 电流和功率等级；
- 开关方式是硬开关还是软开关；
- 导通损耗和开关损耗；
- 驱动器性能；
- 散热条件；
- PCB 布局；
- 电磁干扰；
- 器件成本。




## 10. IGBT

IGBT 的全称是绝缘栅双极型晶体管。

它的三个主要端子是：

- 栅极 Gate，记为 $G$；
- 集电极 Collector，记为 $C$；
- 发射极 Emitter，记为 $E$。

IGBT 的控制端具有类似 MOS 栅极的绝缘结构，因此同样主要通过栅极—发射极电压控制：

$$
V_{\mathrm{GE}}
=
V_G-V_E
$$

从应用角度看，IGBT 同样是一种电压控制型功率器件。

但是，IGBT 的主电流导电机理与功率 MOSFET 不完全相同。IGBT 在导通过程中利用双极型载流子参与导电，因此在较高电压和较大功率的应用中具有一定优势，但也会带来关断拖尾电流等现象。

本教程不深入展开其内部半导体结构，主要关注 IGBT 作为功率开关时表现出的外部特性。

---

## 11. IGBT 的基本控制方式

### 11.1 IGBT 关断

当栅极—发射极电压 $V_{\mathrm{GE}}$ 较低时，IGBT 处于关断状态。

此时，集电极与发射极之间应当近似没有电流流过，器件主要承受集电极—发射极电压

$$
V_{\mathrm{CE}}
=
V_C-V_E
$$

### 11.2 IGBT 导通

当 $V_{\mathrm{GE}}$ 达到器件要求的导通驱动电压时，IGBT 进入导通状态。

IGBT 导通时通常不用一个固定的导通电阻描述，而是关注其集电极—发射极饱和压降 $V_{\mathrm{CE(sat)}}$。

其导通损耗可以近似表示为

$$
p_{\mathrm{cond}}
\approx
V_{\mathrm{CE(sat)}}i_C
$$

与 MOSFET 类似，IGBT 数据手册中的栅极阈值电压也只表示器件开始导通，并不代表器件已经进入适合大功率工作的充分导通状态。

实际驱动电压需要根据具体器件的数据手册确定。

---

## 12. IGBT 的输入特性和输出特性

### 12.1 输入特性

IGBT 的栅极同样具有电容特性。

在稳定导通或者稳定关断时，栅极不需要持续提供很大的直流电流；但是在开通和关断过程中，驱动器仍然需要对栅极电荷进行快速充放电。

因此，IGBT 也需要专门的栅极驱动器。

其驱动过程中同样需要考虑：

- 栅极电荷；
- Miller 平台；
- 栅极电阻；
- 驱动器峰值输出电流；
- 栅极回路寄生电感；
- 高 $\mathrm{d}v/\mathrm{d}t$ 引起的误导通；
- 驱动器欠压锁定；
- 短路和过流保护。

### 12.2 输出特性

IGBT 的输出特性通常描述在不同 $V_{\mathrm{GE}}$ 条件下，集电极电流 $I_C$ 与集电极—发射极电压 $V_{\mathrm{CE}}$ 之间的关系。

这个关系依赖于外围器件，因为 $V_{\mathrm{GE}}$ 控制着饱和压降，而不是电流。
### 12.3 关断拖尾电流

IGBT 在导通过程中存在少数载流子的存储效应。

当栅极驱动信号撤去以后，沟道可以较快关闭，但器件内部储存的载流子不能瞬间消失。因此，集电极电流可能在关断后继续维持一段时间并逐渐下降，这种现象称为拖尾电流。

拖尾电流会增加 IGBT 的关断损耗，使其开关频率通常受到比 MOSFET更明显的限制。

这也是 IGBT 与功率 MOSFET 在开关特性上的一个重要区别。



## 13. IGBT 的反并联二极管

IGBT 本身通常不能像常见功率 MOSFET 那样，直接把内部结构形成的二极管作为可用的反向续流路径。

但是，在电机驱动、逆变器和桥式变换器中，负载电流通常具有感性，开关关断后仍然需要续流路径。

因此，IGBT 经常与反并联二极管配合使用。许多 IGBT 模块内部已经集成了反并联续流二极管。

这个二极管的性能同样会影响：

- 换流过程；
- 反向恢复电流；
- 开通损耗；
- 电压尖峰；
- 电磁干扰；
- 整个桥臂的工作可靠性。

所以，在选择 IGBT 模块时，不能只关注 IGBT 本身，还需要同时关注配套续流二极管的特性。

---


## 14. MOSFET 与 IGBT 的基本比较

MOSFET 和 IGBT 都可以作为功率开关，但它们的导通特性和开关特性存在差异。

| 比较项目 | 功率 MOSFET | IGBT |
|---|---|---|
| 控制量 | 栅源电压 $V_{\mathrm{GS}}$ | 栅射电压 $V_{\mathrm{GE}}$ |
| 稳态驱动 | 栅极基本不需要持续大电流 | 栅极基本不需要持续大电流 |
| 动态驱动 | 需要对栅极电荷充放电 | 需要对栅极电荷充放电 |
| 主要导通参数 | $R_{\mathrm{DS(on)}}$ | $V_{\mathrm{CE(sat)}}$ |
| 导通损耗近似 | $I_{\mathrm{RMS}}^2R_{\mathrm{DS(on)}}$ | 与 $V_{\mathrm{CE(sat)}}I_C$ 有关 |
| 关断特性 | 通常没有明显少数载流子拖尾 | 可能存在拖尾电流 |
| 开关频率 | 通常适合较高频率 | 通常更适合中低开关频率和较高功率 |
| 反向电流路径 | 常见器件内部具有体二极管 | 通常需要外加或集成反并联二极管 |
| 常见应用 | DC-DC、开关电源、同步整流、高频变换 | 高压逆变器、电机驱动和大功率变换 |


---

## 15. 开关器件损耗的基本分类

真实开关器件的损耗可以粗略分为以下几类。

### 15.1 导通损耗

MOSFET 的导通损耗通常近似为

$$
P_{\mathrm{cond,MOS}}
\approx
I_{\mathrm{RMS}}^2R_{\mathrm{DS(on)}}
$$

IGBT 的导通损耗则与导通压降和电流有关，可以根据实际电流波形计算

$$
P_{\mathrm{cond,IGBT}}
=
\frac{1}{T}
\int_0^T
v_{\mathrm{CE}}(t)i_C(t)\,\mathrm{d}t
$$

### 15.2 开关损耗

开关损耗来自器件开通和关断过程中电压与电流的重叠。

粗略表示为

$$
P_{\mathrm{sw}}
\approx
\left(E_{\mathrm{on}}+E_{\mathrm{off}}\right)f_s
$$

实际开关能量还会受到以下因素影响：

- 母线电压；
- 开关电流；
- 栅极驱动电压；
- 栅极电阻；
- 结温；
- 负载类型；
- 二极管反向恢复；
- 电路寄生参数。

### 15.3 栅极驱动损耗

栅极驱动器需要反复对栅极电荷充电和放电，其损耗可以粗略估算为

$$
P_{\mathrm{gate}}
\approx
Q_gV_{\mathrm{drv}}f_s
$$

### 15.4 与寄生电容和二极管有关的损耗

器件的输出电容需要在每次开关过程中充电或者放电，续流二极管和体二极管还可能产生反向恢复损耗。

这些损耗在高电压和高开关频率条件下可能变得非常显著。

因此，器件总损耗可以粗略理解为

$$
P_{\mathrm{device}}
\approx
P_{\mathrm{cond}}
+
P_{\mathrm{sw}}
+
P_{\mathrm{gate}}
+
P_{\mathrm{other}}
$$

其中，$P_{\mathrm{other}}$ 可以包括输出电容损耗、反向恢复损耗、漏电损耗以及其他寄生效应引起的损耗。

