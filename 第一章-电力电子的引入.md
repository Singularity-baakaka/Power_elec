# 电力电子的引入：为什么需要开关变换

## 1. 为什么需要电力电子

在正式分析各种变换器之前，我们首先需要回答一个最基本的问题：

> **为什么需要电力电子？**

电压变换并不是电力电子出现以后才产生的需求。在传统认识中，实现电压变换主要有以下两类方法：

1. 使用变压器；
2. 使用电阻、晶体管和运算放大器等线性元件，搭建有源或无源调节电路。

这两种方法各有用途，但也存在明显的限制。

---

## 2. 使用变压器进行电压变换

第一种方法是使用变压器。

当变压器原边绕组中的电流随时间变化时，它会在磁芯中建立变化的磁通，并在副边绕组中感应出电压。通过选择原边和副边的匝数比，便可以实现升压、降压以及电气隔离。

理想情况下，变压器可以高效率地传递能量。但是，变压器依赖于**随时间变化的磁通**。对于一个恒定不变的直流电压，稳态时磁通不再变化，副边也就不能持续产生感应电压。

因此，变压器不能直接完成稳态直流电压之间的变换。

若希望利用变压器变换直流电压，就必须先把直流转换成随时间变化的电压，再通过变压器传递能量，最后重新进行整流和滤波。事实上，这已经开始进入开关电源的范畴。

---

## 3. 使用线性元件进行电压调节

第二种方法是使用电阻、晶体管和运算放大器等线性元件，搭建有源或无源调节电路。

### 3.1 电阻分压

最简单的例子是电阻分压。

两个电阻串联后，可以从中间节点得到一个较低的电压。若暂时不考虑负载，其输出电压为

$$
V_o
=
V_{\mathrm{in}}
\frac{R_2}{R_1+R_2}
$$

式中：

- $V_{\mathrm{in}}$ 是输入电压；
- $V_o$ 是输出电压；
- $R_1$ 和 $R_2$ 是组成分压网络的两个电阻。

如果输出节点几乎不向外提供电流，那么它确实可以得到一个由电阻比例决定的电压。

但是，当负载电阻 $R_L$ 接入以后，$R_L$ 会与 $R_2$ 并联，实际输出电压变为

$$
V_o
=
V_{\mathrm{in}}
\frac{R_2\parallel R_L}
{R_1+R_2\parallel R_L}
$$

这时，输出电压不仅与 $R_1$ 和 $R_2$ 有关，还会随着负载 $R_L$ 的变化而变化。

这就是电阻分压电路的**负载效应**。

与此同时，分压电阻还会持续消耗功率。因此，电阻分压适合处理小信号或者产生参考电压，却不适合作为向大功率负载供电的电源。

### 3.2 线性稳压

为了改善负载变化对输出电压的影响，我们可以加入运算放大器和功率管，通过负反馈不断调节功率管的工作状态，使输出电压稳定在给定值附近。

这就是线性稳压的一种基本思想。

在典型的串联型线性稳压器中，功率管工作在线性区域。我们可以把它直观地理解为一个受到负反馈控制的“可变压降元件（可变电阻）”。

假设：

- 输入电压为 $V_{\mathrm{in}}$；
- 输出电压为 $V_o$；
- 负载电流为 $I_o$。

那么，功率管两端大约需要承受

$$
V_{\mathrm{in}}-V_o
$$

的电压，其功率损耗近似为

$$
P_{\mathrm{loss}}
\approx
\left(V_{\mathrm{in}}-V_o\right)I_o
$$

这部分能量最终主要转化为热量。

例如，若使用 $12\ \mathrm{V}$ 的输入电压产生 $5\ \mathrm{V}$、$1\ \mathrm{A}$ 的输出，则负载得到的功率为

$$
P_o
=
V_oI_o
=
5\times1
=
5\ \mathrm{W}
$$

而调整管上的功率损耗约为

$$
P_{\mathrm{loss}}
=
(12-5)\times1
=
7\ \mathrm{W}
$$

此时，调整管消耗的功率甚至超过了负载实际得到的功率。

因此，线性调节方式虽然结构直观、输出平滑、噪声较低，但当输入输出压差较大或者负载功率较高时，会产生显著的能量损耗和散热压力。

问题由此产生：

> 能否不让功率器件长期工作在线性区域并持续承受压降，而是采用另一种方式，实现高效率的电压变换？

这正是开关变换思想的出发点。

---

## 4. 从水池理解开关调节

为了建立直观认识，我们先考虑一个水池。

水池具有一根排水管，它以固定流量向外排水；同时，水池还有一根进水管。我们的目标是让水池中的水位保持在指定高度附近。

现在增加以下限制：

- 进水管只能全开或者全关；
- 进水管全开时的流量，恰好是排水流量的两倍。

设进水管全开时的流量为 $Q_{\mathrm{in}}$，排水流量为 $Q_{\mathrm{out}}$，则有

$$
Q_{\mathrm{in}}
=
2Q_{\mathrm{out}}
$$

如果进水管始终关闭，水池中的水会逐渐减少，水位不断下降。

如果进水管始终全开，那么流入的水比流出的水多，水池中的水会逐渐增加，水位不断上升。

那么，我们应该怎样让水位保持稳定？

一种可行的方法是：

1. 将进水管打开一段时间；
2. 再将进水管关闭一段时间；
3. 周期性地重复这一过程。

虽然进水管在任意时刻都只有“全开”和“全关”两种状态，但是，只要合理控制一个周期内的开启时间，便可以控制进水管的**平均进水流量**。

---

## 5. 占空比与平均作用

设一个开关周期为 $T_s$，进水管在每个周期内开启的时间为 $T_{\mathrm{on}}$。

定义占空比

$$
D
=
\frac{T_{\mathrm{on}}}{T_s}
$$

占空比 $D$ 的取值范围为

$$
0\leq D\leq1
$$

如果进水管全开时的流量为 $Q_{\mathrm{in}}$，那么一个周期内的平均进水流量为

$$
\overline{Q}_{\mathrm{in}}
=
DQ_{\mathrm{in}}
$$

为了使水池在长期平均意义下既不增加水量，也不减少水量，需要满足

$$
DQ_{\mathrm{in}}
=
Q_{\mathrm{out}}
$$

因此，所需占空比为

$$
D
=
\frac{Q_{\mathrm{out}}}{Q_{\mathrm{in}}}
$$

在本例中，因为

$$
Q_{\mathrm{in}}
=
2Q_{\mathrm{out}}
$$

所以

$$
D
=
\frac{1}{2}
$$

也就是说，只要在每个周期内让进水管开启一半时间、关闭一半时间，平均流入水池的水量就会等于平均排出的水量。

此时，水位并不会完全静止：

- 进水管打开时，流入量大于流出量，水位略微升高；
- 进水管关闭时，水池只向外排水，水位略微降低。

因此，水面会在目标高度附近产生小幅度的周期性起伏。

但是，只要一个周期内流入和流出的水量相等，水位的平均值就可以保持不变。

---

## 6. 提高开关频率可以减小波动

设水池的水平截面积为 $A$，水位高度为 $h$。

忽略其他复杂因素，可以写出水池的动态方程：

$$
A\frac{\mathrm{d}h}{\mathrm{d}t}
=
Q_{\mathrm{in}}s(t)-Q_{\mathrm{out}}
$$

式中，$s(t)$ 表示进水管的开关状态：

$$
s(t)
=
\begin{cases}
1, & \text{进水管全开}\\
0, & \text{进水管关闭}
\end{cases}
$$

这个方程说明，水位的变化速度取决于流入流量与流出流量之差，同时还与水池的截面积有关。

当我们缩短开关周期，也就是提高开关频率时，每次开启或关闭持续的时间都会缩短。

水位还没有来得及发生很大的变化，进水管的状态就已经发生了切换。因此，在其他条件不变时，水位的起伏通常会减小。

类似地，如果水池的截面积 $A$ 更大，那么同样数量的水流入或者流出时，引起的水位变化也会更小。

因此：

- 开关频率越高，单个周期内的水位起伏通常越小；
- 水池面积越大，水位对短时间流量变化越不敏感。

最终，我们得到了一个非常重要的结果：

> 进水管在任意瞬间仍然只有“全开”和“全关”两种状态，但从较长的时间尺度观察，它却表现得像一个开度连续可调的阀门。

需要注意，这里所谓的“连续可调”，指的是进水管的**平均作用连续可调**，并不是说它的瞬时状态真的变成了连续状态。

这就是脉宽调制的基本思想：

> 通过改变开关在一个周期内的导通时间比例，使一个只有两种瞬时状态的开关元件产生连续可调的平均效果。

---

## 7. 惯性器件为什么重要

水池之所以能够将断续的进水过程转化为相对平缓的水位变化，是因为水池能够储存水。

水池当前的水位不仅由此刻的进水状态决定，也与过去一段时间内流入和流出的水量有关。

因此，水池可以看作一个具有“记忆”的系统。


在电路中，承担类似作用的主要是两类储能元件：

- **电容器**储存电场能量；
- **电感器**储存磁场能量。


对于某些用电器，它不能承受忽高忽低的电压，这意味着，假设我们有个开关，不能直接把它接在用电器上，而是需要一种记忆器件，这种记忆期间就像刚刚的水池一样，它的输出是平缓的（排水管），设计电源，相当于找出一种结构，能在我们的控制下，精准为用电器输出平缓的电压。

注意，在之后的学习中，我们可不能真的把电容电感看作水池，水池的输出口的流量恒定，而真正的电容，电感是单端口器件，充放过程是同步进行的。

### 7.1 电容器的记忆性

电容器满足

$$
i_C
=
C\frac{\mathrm{d}v_C}{\mathrm{d}t}
$$

也可以写成

$$
\frac{\mathrm{d}v_C}{\mathrm{d}t}
=
\frac{i_C}{C}
$$

这说明，电容电压的变化速度取决于流入电容的电流。

如果希望电容电压在极短时间内发生很大的变化，就需要非常大的电流。对于有限的电流，电容电压只能逐渐变化。

因此，电容具有保持电压连续变化的趋势。

### 7.2 电感器的记忆性

电感器满足

$$
v_L
=
L\frac{\mathrm{d}i_L}{\mathrm{d}t}
$$

也可以写成

$$
\frac{\mathrm{d}i_L}{\mathrm{d}t}
=
\frac{v_L}{L}
$$

这说明，电感电流的变化速度取决于电感两端的电压。

如果希望电感电流在极短时间内发生很大的变化，就需要非常大的电压。对于有限的电压，电感电流同样只能逐渐变化。

因此，电感具有保持电流连续变化的趋势。

这就是电感和电容所具有的“记忆性”：

- 电容会延续此前建立的电压状态；
- 电感会延续此前建立的电流状态。

开关元件负责快速改变电路的连接状态，电感和电容负责储存、释放并平滑传递能量。

两者结合起来，便可以把开关元件的离散状态转化为近似连续、可以调节的平均电压或者平均电流。

可以把这种思想概括为：

> **用开关决定能量从哪里流动，用储能元件连接相邻的开关状态，再用占空比控制一段时间内传递的平均能量。**

这将是后续分析各种电力电子变换器时最重要的思想之一。

我们需要补充一点：在某些教程中，可能会引入所谓“滤波器”的概念。它的思想是，把方波中的高次谐波滤除，得到一个直流分量。

这个方波是由开关产生的、带偏置的方波。我们把高频谐波滤去，得到直流分量。这种思想固然有一定合理性，但它只适用于某些特定拓扑。

在某些拓扑中，开关开断的过程往往意味着整个电路结构的变化，在这种情况下。不能再把开关的输出看作一个方波电压源。而要从记忆元件的角度去分析了。

当然，这种分析方式在某些情况下仍旧是有用的，例如设计全桥逆变器的输出LC网络，从而得到平滑的正弦波。这个设计过程会使用这种滤波器的思想，因为全桥拓扑本身允许被这样分析，开关过程不带来电路结构性的变化。




---

## 8. 为什么开关方式能够提高效率

采用开关方式并不只是为了获得可调的平均效果，更重要的是降低功率器件自身的损耗。

任意器件的瞬时功率为

$$
p(t)
=
v(t)i(t)
$$

理想开关具有导通和关断两种状态。

### 开关导通

理想开关导通时，其两端电压为零：

$$
v_{\mathrm{sw}}
=
0
$$

即使此时流过较大的电流，开关损耗仍然为

$$
p_{\mathrm{sw}}
=
v_{\mathrm{sw}}i_{\mathrm{sw}}
=
0
$$

### 开关关断

理想开关关断时，流过它的电流为零：

$$
i_{\mathrm{sw}}
=
0
$$

即使此时开关两端承受较高的电压，其损耗仍然为

$$
p_{\mathrm{sw}}
=
v_{\mathrm{sw}}i_{\mathrm{sw}}
=
0
$$

因此，理想开关无论处于导通还是关断状态，都不消耗功率。

这与线性调节方式形成了鲜明对比。

在线性调节过程中，功率管可能同时承受较大的电压和电流，于是产生较大的功率损耗；而在开关调节中，我们尽量让功率器件只停留在以下两类状态：

- 低电压、大电流的导通状态；
- 高电压、小电流的关断状态。

这样可以尽量避免器件同时承受较大的电压和电流。

实际开关器件当然不是理想的：

- 导通时存在导通电阻或者导通压降；
- 关断时存在漏电流；
- 开通和关断转换需要一定时间；
- 在开关转换过程中，器件可能同时承受电压和电流；
- 提高开关频率还会增加开关损耗、驱动损耗和电磁干扰。

所以，开关频率并不是越高越好。

设计者必须在以下因素之间进行折中：

- 电压和电流纹波；
- 电感、电容和变压器的体积；
- 系统动态响应；
- 导通损耗；
- 开关损耗；
- 器件温升；
- 电磁干扰；
- 控制和驱动的实现难度。

但是，开关方式仍然提供了一条实现高效率电能变换的基本路径：

> 不再通过功率器件持续消耗多余能量来调节输出，而是控制能量何时传递、沿什么路径传递、传递多长时间，以及每个周期传递多少能量。



---

## 9. 电力电子研究什么

在建立上述认识以后，我们便可以更加准确地理解电力电子所研究的问题。

电力电子并不只是研究“怎样让某个开关管导通”，而是研究如何利用功率半导体器件、电感、电容和变压器，对电能进行高效率的变换、传递和控制。

它通常包括以下几个相互关联的方面。

### 9.1 变换器拓扑

首先需要研究：

- 应该怎样连接开关管、二极管、电感和电容；
- 不同连接方式可以实现升压、降压还是反相；
- 能量在不同开关状态下沿什么路径流动；
- 是否需要电气隔离；
- 能量能否双向流动。

这些问题共同构成变换器的**拓扑问题**。

### 9.2 输入输出特性

确定拓扑以后，还需要分析：

- 输出电压与输入电压有什么关系；
- 占空比如何影响输出；
- 负载变化会怎样影响工作状态；
- 电感电流是否连续；
- 输出会产生多大的电压和电流纹波；
- 输入端表现出怎样的电气特性。

这部分建立的是变换器的稳态输入输出关系。

### 9.3 器件应力与效率

实际设计还必须考虑：

- 开关管承受的最大电压和最大电流；
- 二极管承受的反向电压和平均电流；
- 电感是否可能进入磁饱和；
- 电容需要承受多大的纹波电流；
- 导通损耗和开关损耗分别有多大；
- 磁性器件和散热系统应该如何设计。

这些问题决定一个变换器是否能够安全、可靠并高效率地工作。

### 9.4 动态过程与稳定性

变换器并不总是工作在完全不变的条件下。

当输入电压发生变化、负载发生突变或者参考值改变时，输出都会经历一个动态过程。因此，我们还需要研究：

- 电感电流和电容电压如何随时间变化；
- 输出能否回到目标值；
- 系统是否会产生持续振荡；
- 闭环控制系统是否稳定；
- 瞬态响应是否足够快；
- 输出的超调和跌落是否可以接受。

### 9.5 控制与保护

为了使变换器按照要求工作，还需要设计相应的控制系统和保护功能，例如：

- 电压闭环；
- 电流闭环；
- PWM 调制；
- 软启动；
- 限流；
- 过压保护；
- 欠压保护；
- 过温保护；
- 桥臂死区；
- 故障关断。

因此，电力电子实际上处在以下三个领域的交叉位置：

1. **功率电路**：研究能量经过哪些器件和路径传递；
2. **电子器件**：研究功率半导体和储能元件的实际特性；
3. **自动控制**：研究如何使输出按照目标稳定地变化。

---

## 10. 本教程的主要范围

按照输入和输出电能形式的不同，电力电子变换器通常可以分为：

- DC-DC：直流到直流；
- AC-DC：交流到直流；
- DC-AC：直流到交流；
- AC-AC：交流到交流。

本教程将以 **DC-DC 变换器**为主要研究对象。因为 DC-DC 变换器最适合用来建立电力电子中的基础分析方法。

通过学习 DC-DC 变换器，我们可以逐步掌握：

- 如何枚举开关状态；
- 如何判断每个状态下的电流路径；
- 如何分析电感的储能与放能；
- 如何分析电容的充电与放电；
- 如何利用电感伏秒平衡；
- 如何利用电容电荷平衡；
- 如何推导占空比与输出电压的关系；
- 如何计算电压和电流纹波；
- 如何区分连续导通模式和断续导通模式；
- 如何建立变换器的平均模型；
- 如何设计闭环控制器；
- 如何分析器件应力和功率损耗。

这些方法并不会局限于 DC-DC 变换器。

在学习 AC-DC 变换器时，我们仍然需要分析开关状态、电感电流和直流母线；在学习 DC-AC 逆变器时，我们仍然需要通过桥臂开关状态和 PWM 控制平均输出电压；在学习 AC-AC 变换器时，也仍然需要处理换流、储能和周期平均问题。

当我们真正理解以下这条主线：

> **开关状态改变能量路径，储能元件延续系统状态，占空比控制平均能量传递速率。**

再去学习 AC-DC、DC-AC 和 AC-AC 变换器时，就不会把它们看成一组彼此孤立的新电路，而会发现它们都在重复使用同一套基本思想。

