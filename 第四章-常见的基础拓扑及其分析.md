# 常见的基础拓扑及其分析

本章讨论若干最常见的 DC-DC 变换器拓扑，并建立一套统一的分析方法。


本章主要讨论六类基础拓扑：

1. Buck；
2. Boost；
3. 基础反相 Buck-Boost；
4. Flyback；
5. Forward；
6. 桥式隔离型变换器，包括 Half-Bridge 与 Full-Bridge。

---

## 1. 本章的分析方法

对于每一个拓扑，我们依次讨论以下内容：

1. **电路目标**：该拓扑实现降压、升压、升降压，还是隔离变换；
2. **开关状态**：一个周期内电路经历哪些状态，开关和二极管分别如何导通或截止；
3. **能量变化**：输入电源、电感、电容、励磁电感、变压器以及负载之间，能量如何流动；
4. **PSS 条件**：进入周期稳态后，哪些状态量在一个周期结束时回到原值；
5. **输入输出特性**：利用 PSS 条件推导平均输入电压与平均输出电压之间的关系。

本章暂不推导纹波的具体大小，电流模式、器件应力和损耗。

希望读者们能够在实战的时候，利用这些分析方法手动去推导每个器件上的电流，电压，从而分析应力等。而对于电流模式如CCM,DCM这些，在这里讨论的意义不大，因为无论什么模式，依旧遵循PSS，没有必要对每个模式再进行分析。

对于纹波，只需要引入小纹波近似，对电流，直接分析电感两端的电压便可得到电流的导数，再结合负载电流

为避免引入过多非理想因素，本章中的推导默认满足以下条件：

- 开关、二极管、电感、电容和变压器均视为理想器件；
- 输入电压恒定，记为 $V_{\mathrm{in}}$；
- 输出电压的平均值记为 $V_o$；
- 开关周期为 $T_s$，开关频率为 $f_s=1/T_s$；
- 主开关导通时间为 $DT_s$，其中 $D$ 为占空比；
- 对含输出电感的拓扑，默认工作在连续电流模式（即输出电感的电流永远大于0）；
- 为得到简洁的直流变换比，默认输出电压纹波相对较小，可用平均输出电压 $V_o$ 代表一个周期内的输出电压（小纹波近似）。

---

## 2. PSS 周期稳态条件回顾

在第三章已经叙述了，这里请各位回顾一下，下面的分析将一直用到它们。


---

## 3. Buck 变换器

### 3.1 电路目标

Buck 变换器用于将较高的直流输入电压变换为较低的直流输出电压。

其基本特点是：输入电源在主开关导通时直接向输出侧传递能量；输出电感用于维持连续电流，并在主开关关断时继续向负载供能。

![[buck_conv.png]]


---

### 3.2 状态一：主开关导通

在 $0<t<DT_s$ 内：

- 主开关 $Q_1$ 导通；
- 续流二极管 $D_1$ 截止；
- 输入电源经主开关和电感向负载供电；
- 输入电源同时向电感中储存磁场能量；
- 电感电流上升。
- 这取稳定之后的某一个周期，因此电容的电压为$V_o$

此时电感两端电压为

$$
v_L=V_{\mathrm{in}}-V_o
$$

由于有 $V_{\mathrm{in}}>V_o$，所以

$$
\frac{\mathrm{d}i_L}{\mathrm{d}t}>0
$$

电感电流上升，电感中的磁场能量增加。

---

### 3.3 状态二：主开关关断

在 $DT_s<t<T_s$ 内：

- 主开关 $Q_1$ 截止；
- 续流二极管 $D_1$ 导通；
- 输入电源与输出侧断开；
- 电感电流不能突变，因此电感通过续流二极管维持原方向电流；
- 电感向负载和输出电容释放此前储存的部分能量；
- 电感电流下降。

此时电感两端电压为

$$
v_L=-V_o
$$

因此，

$$
\frac{\mathrm{d}i_L}{\mathrm{d}t}<0
$$

---

### 3.4 PSS 条件与输入输出关系

在 PSS 条件下，电感满足伏秒平衡：

$$
D(V_{\mathrm{in}}-V_o)
+
(1-D)(-V_o)
=
0
$$

整理得

$$
V_o=DV_{\mathrm{in}}
$$

因此，Buck 变换器的理想电压变换比为

$$
\boxed{
\frac{V_o}{V_{\mathrm{in}}}=D
}
$$

由于 $0<D<1$，所以 Buck 只能实现降压。

---

## 4. Boost 变换器

### 4.1 电路目标

Boost 变换器用于将较低的直流输入电压变换为较高的直流输出电压。

其基本特点是：主开关导通时，输入电源首先向电感储能；主开关关断时，输入电源与电感共同向输出侧传递能量。

![[boost_conv.png]]

---

### 4.2 状态一：主开关导通

在 $0<t<DT_s$ 内：

- 主开关 $Q_1$ 导通；
- 输出二极管 $D_1$ 截止；
- 输入电源直接向电感供能；
- 电感电流上升；
- 输出侧暂时与输入侧断开，负载主要由输出电容供电。

此时，

$$
v_L=V_{\mathrm{in}}
$$

因此，

$$
\frac{\mathrm{d}i_L}{\mathrm{d}t}>0
$$

输入电源持续向电感的磁场中储存能量。

---

### 4.3 状态二：主开关关断

在 $DT_s<t<T_s$ 内：

- 主开关 $Q_1$ 截止；
- 输出二极管 $D_1$ 导通；
- 电感电流不能突变；
- 电感产生相应的电压，使二极管导通；
- 输入电源与电感共同向输出电容和负载传递能量；
- 电感电流下降。

此时，

$$
v_L=V_{\mathrm{in}}-V_o
$$

由于 Boost 工作时通常有 $V_o>V_{\mathrm{in}}$，因此

$$
v_L<0
$$

电感开始释放能量。

---

### 4.4 PSS 条件与输入输出关系

对电感应用伏秒平衡：

$$
DV_{\mathrm{in}}
+
(1-D)(V_{\mathrm{in}}-V_o)
=
0
$$

整理得

$$
V_o=\frac{V_{\mathrm{in}}}{1-D}
$$

因此，Boost 变换器的理想电压变换比为

$$
\boxed{
\frac{V_o}{V_{\mathrm{in}}}
=
\frac{1}{1-D}
}
$$

由于 $0<D<1$，所以理想 Boost 变换器满足

$$
V_o>V_{\mathrm{in}}
$$

即它只能实现升压。

---

## 5. 基础反相 Buck-Boost 变换器

### 5.1 电路目标

基础反相 Buck-Boost 变换器可以实现升压或降压，但输出电压极性与输入电压极性相反。

它与 Boost 一样具有“先储能、后放能”的特点，但其输出侧在主开关导通时与输入侧断开。
![[buck_boost_conv.png]]
---

### 5.2 状态一：主开关导通

在 $0<t<DT_s$ 内：

- 主开关 $Q_1$ 导通；
- 输出二极管 $D_1$ 截止；
- 输入电源向电感供能；
- 电感电流上升；
- 输出电容独立向负载供电。

此时，

$$
v_L=V_{\mathrm{in}}
$$

因此，电感吸收能量。

---

### 5.3 状态二：主开关关断

在 $DT_s<t<T_s$ 内：

- 主开关 $Q_1$ 截止；
- 输出二极管 $D_1$ 导通；
- 电感电流维持原方向；
- 电感两端电压极性改变；
- 电感向输出电容和负载释放能量；
- 输出端形成与输入端相反的极性。

若将输出电压定义为输出端相对于输入负端的有符号电压，则有

$$
V_o<0
$$

此时，

$$
v_L=V_o
$$

由于 $V_o<0$，所以电感电流下降。

---

### 5.4 PSS 条件与输入输出关系

电感伏秒平衡为

$$
DV_{\mathrm{in}}
+
(1-D)V_o
=
0
$$

因此，

$$
V_o
=
-\frac{D}{1-D}V_{\mathrm{in}}
$$

即

$$
\boxed{
\frac{V_o}{V_{\mathrm{in}}}
=
-\frac{D}{1-D}
}
$$

负号表示输出极性反向。

当

$$
D<\frac{1}{2}
$$

时，输出电压幅值小于输入电压幅值；当

$$
D>\frac{1}{2}
$$

时，输出电压幅值大于输入电压幅值。

---

## 6. Flyback 变换器

### 6.1 电路目标

Flyback 变换器用于需要电气隔离的升压、降压或升降压场合。

Flyback 中的磁性元件既具有原副边耦合关系，又具有明显的电感储能作用。它不能被理解为普通变压器,应当把它理解为一个耦合电感，磁链存在于整个耦合电感里，通过磁链存储的能量完成变换；从能量传递方式看，它更接近一个具有隔离功能的 Buck-Boost 变换器。
![[flyback_conv.png]]

---

### 6.2 状态一：主开关导通

在 $0<t<DT_s$ 内：

- 原边主开关 $Q_1$ 导通；
- 原边输入电压施加到励磁电感；
- 原边励磁电流 $i_m$ 上升；
- 副边感应出电压，副边的同名端在下方，所以$D_1$关断
- 输出侧暂时不从原边获得能量，负载主要由输出电容供电。

此时，

$$
v_{L_m}=V_{\mathrm{in}}
$$

因此，

$$
\frac{\mathrm{d}i_m}{\mathrm{d}t}>0
$$

励磁电感的磁场能量增加。

在这一阶段中，原边磁链逐渐增加。在固定电压作用下，磁链随时间线性变化。

---

### 6.3 状态二：主开关关断

在 $DT_s<t<T_s$ 内：

- 主开关 $Q_1$ 截止；
- 整个耦合电感中存在着磁链
- 由于电磁感应，原边绕组电压极性翻转，在副边回路维持励磁电流，励磁电流大小符合安匝熟（这在下一节的磁件中讲到）；
- 二极管导通；
- 电感中此前储存的磁场能量，经副边二极管递到输出电容和负载；
- 励磁电流下降，磁链逐渐回落,注意，由于输出电压$V_O$近似不变，所以，这个磁链的下降过程也是线性的，励磁电流的下降相应也是线性的。

这一过程的关键是：

> 在开关关断的瞬间，励磁电流和磁链具有非零初值，不能突然消失。为了维持电流连续性，磁性元件两端电压改变极性，电路自动切换到副边放能状态。

设变压器匝比为

$$
n=\frac{N_s}{N_p}
$$

忽略二极管压降时，关断阶段折算到原边的励磁电感电压可写为

$$
v_{L_m}
=
-\frac{N_p}{N_s}V_o
=
-\frac{V_o}{n}
$$

---

### 6.4 PSS 条件与输入输出关系

Flyback 的励磁电流和原边磁链均应满足 PSS：

$$
i_m(t+T_s)=i_m(t)
$$

$$
\lambda_p(t+T_s)=\lambda_p(t)
$$

因此，对这一对耦合电感应用伏秒平衡（磁链同时存在于整个耦合电感内）：

$$
DV_{\mathrm{in}}
+
(1-D)
\left(
-\frac{V_o}{n}
\right)
=
0
$$

整理得

$$
V_o
=
n\frac{D}{1-D}V_{\mathrm{in}}
$$

即

$$
\boxed{
\frac{V_o}{V_{\mathrm{in}}}
=
\frac{N_s}{N_p}
\frac{D}{1-D}
}
$$

Flyback 的形式与 Buck-Boost 十分接近，只是额外引入了变压器匝比和电气隔离。

---

## 7. Forward 变换器

### 7.1 电路目标

Forward 变换器同样用于隔离型 DC-DC 变换，但其能量传递方式与 Flyback 不同。

Forward 的主要特点是：

- 主开关导通时，输入能量经变压器直接传递到副边；
- 输出电感承担主要的连续储能任务；
- 变压器主要承担隔离与变比功能；
- 原边磁链需要在关断阶段被复位（现实中的变压器非理想，相当于在理想变压器上并联了一个励磁电感，对于这个拓扑，输入的电压可以看作交流方波加一个直流偏置）。

因此，Forward 可以理解为隔离型 Buck。
![[forward_conv.png]]

---

### 7.2 状态一：主开关导通

在 $0<t<DT_s$ 内：

- 主开关 $Q_1$ 导通；
- 原边绕组施加输入电压；
- 副边绕组立即感应出对应电压（同名端在上方）；
- 副边主整流二极管$D_1$导通；
- 输入能量经变压器、副边整流器和输出电感直接传递到负载；
- 输出电感电流上升；
- 原边励磁电流上升,但这里的励磁电流较小，因为Forward拓扑这里是真的一个近似理想的变压器，只需要很小的励磁电流。

设匝比为

$$
n=\frac{N_s}{N_p}
$$

则输出电感电压为

$$
v_{L_o}
=
nV_{\mathrm{in}}-V_o
$$

此时，输出电感吸收能量或增加其储能。



---

### 7.3 状态二：主开关关断

在 $DT_s<t<T_s$ 内：

- 主开关 $Q_1$ 截止；
- 副边主整流二极管截止；
- 输出电感电流不能突变；
- 续流二极管导通；
- 输出电感继续向负载和输出电容供能；
- 输出电感电流下降；
- 复位绕组、箝位支路或其他复位结构，使原边获得反向电压；
- 原边磁链回落，为下一周期做准备。

输出电感两端电压为

$$
v_{L_o}=-V_o
$$

对于变压器而言，关断阶段的关键任务是复位。若原边磁链没有在下一次导通前回到合适范围，则磁链会逐周期累积，最终导致磁芯饱和。

---

### 7.4 PSS 条件与输入输出关系

对输出电感应用伏秒平衡：

$$
D(nV_{\mathrm{in}}-V_o)
+
(1-D)(-V_o)
=
0
$$

整理得

$$
V_o=nDV_{\mathrm{in}}
$$

即

$$
\boxed{
\frac{V_o}{V_{\mathrm{in}}}
=
\frac{N_s}{N_p}D
}
$$

此外，Forward 的原边磁链还必须满足伏秒平衡。输出电压关系由输出电感伏秒平衡得到；磁芯是否能够长期正常工作，则取决于原边复位是否充分。

---

## 8. 桥式隔离型变换器

桥式隔离型变换器通过多个开关器件组成桥臂，在变压器原边施加正、负交替的电压。

与单端 Forward 不同，桥式变换器通常不依赖额外的复位绕组来使磁链回到原值。原边电压本身的正负交替，就可以使磁链在两个方向之间摆动。

桥式拓扑的输出侧通常包括：

- 高频变压器；
- 副边整流器；
- 输出电感；
- 输出电容和负载。

因此，桥式变换器的输出侧仍然具有类似 Buck 的特征：整流后的电压经输出电感传递到负载。

---

### 8.1 Half-Bridge

#### 电路目标

Half-Bridge 使用两个串联开关器件和母线分压电容，为变压器原边提供双极性激励，这样就不再需要复位绕组。

在理想情况下，原边电压幅值约为

$$
\frac{V_{\mathrm{in}}}{2}
$$
![[half_bridge.png]]

---

#### 开关状态

Half-Bridge 的两个主开关互补工作，并需要设置死区时间。

当上管导通时：

- 原边获得一个方向的电压；
- 原边磁链向一个方向变化；
- 副边向输出电感、输出电容和负载传递能量。

当下管导通时：

- 原边获得相反方向的电压；
- 原边磁链向相反方向变化；
- 副边电压极性也随之翻转；
- 由于同名端的位置，输出侧仍然获得同一极性的能量。

在这一过程中，变压器原边并不是先在一个完整周期内持续积累磁链，再统一释放能量；而是在正、负两个原边电压区间内，磁链分别向两个方向摆动。

---

#### PSS 条件

若正、负原边电压幅值均为 $V_{\mathrm{in}}/2$，则原边伏秒平衡要求

$$
\frac{V_{\mathrm{in}}}{2}t_+
-
\frac{V_{\mathrm{in}}}{2}t_-
=
0
$$

即

$$
t_+=t_-
$$

其中，$t_+$ 和 $t_-$ 分别为原边承受正、负电压的时间。

---

#### 输入输出特性

设

$$
n=\frac{N_s}{N_p}
$$

并定义 $D_{\mathrm{eff}}$ 为副边经过整流后、实际向输出电感传递能量的有效占空比，则理想情况下可写为

$$
V_o
=
D_{\mathrm{eff}}
\frac{N_s}{N_p}
\frac{V_{\mathrm{in}}}{2}
$$

即

$$
\boxed{
\frac{V_o}{V_{\mathrm{in}}}
=
D_{\mathrm{eff}}
\frac{N_s}{N_p}
\frac{1}{2}
}
$$

这里的 $D_{\mathrm{eff}}$ 不一定等于某一个 MOSFET 的单独导通占空比，而应由具体的调制方式定义。

---

### 8.2 Full-Bridge

#### 电路目标

Full-Bridge 使用四个主开关构成 H 桥。通过两组对角开关交替导通，变压器原边可以获得完整母线电压的正、负激励。

理想情况下，原边电压可在以下两个值之间切换：

$$
+V_{\mathrm{in}}
$$

和

$$
-V_{\mathrm{in}}
$$

相较于 Half-Bridge，Full-Bridge 能够在原边施加更高的电压，因此更适合较高功率场合。

![[full_bridge.png]]

---

#### 开关状态

当一组对角开关导通时，例如 $Q_1$ 与 $Q_4$ 导通：

- 原边承受正向电压；
- 原边磁链向正方向变化；
- 副边向输出侧传递能量。

当另一组对角开关导通时，例如 $Q_2$ 与 $Q_3$ 导通：

- 原边承受反向电压；
- 原边磁链向反方向变化；
- 副边电压极性翻转；
- 输出电感电流仍保持同一方向。

在两个桥臂换流的瞬间，必须设置死区时间，以避免同一桥臂的上下开关同时导通而造成直通。

---

#### PSS 条件

若正、负原边电压幅值均为 $V_{\mathrm{in}}$，则原边伏秒平衡要求

$$
V_{\mathrm{in}}t_+
-
V_{\mathrm{in}}t_-
=
0
$$

因此，

$$
t_+=t_-
$$

这意味着正向与反向激励在一个完整周期内应当保持伏秒平衡，避免原边磁链持续偏移。

---

#### 输入输出特性

仍定义

$$
n=\frac{N_s}{N_p}
$$

以及有效传能占空比 $D_{\mathrm{eff}}$。

在理想情况下，

$$
V_o
=
D_{\mathrm{eff}}
\frac{N_s}{N_p}
V_{\mathrm{in}}
$$

即

$$
\boxed{
\frac{V_o}{V_{\mathrm{in}}}
=
D_{\mathrm{eff}}
\frac{N_s}{N_p}
}
$$

Full-Bridge 的具体 $D_{\mathrm{eff}}$ 定义取决于控制方式。对于普通 PWM、移相全桥、不同整流结构以及不同零电压区间，$D_{\mathrm{eff}}$ 的含义并不完全相同。

这并不是本章的主要内容，只需把它当作对普通Forward的改进。

---

## 9. 小结

本章中的各类拓扑虽然结构不同，但分析步骤是一致的：

$$
\text{明确电路目标}
\rightarrow
\text{划分开关状态}
\rightarrow
\text{分析器件导通关系}
\rightarrow
\text{分析能量路径}
\rightarrow
\text{写出电感或磁链的状态变化}
\rightarrow
\text{施加 PSS 条件}
\rightarrow
\text{得到输入输出关系}
$$

各拓扑的主要特征如下：

| 拓扑 | 主要功能 | 主要磁件作用 | 能量传递方式 | 理想输入输出关系 |
|---|---|---|---|---|
| Buck | 降压 | 输出电感储能 | 导通时直接传能 | $\displaystyle \frac{V_o}{V_{\mathrm{in}}}=D$ |
| Boost | 升压 | 电感储能 | 先储能，后放能 | $\displaystyle \frac{V_o}{V_{\mathrm{in}}}=\frac{1}{1-D}$ |
| Buck-Boost | 升压或降压 | 电感储能 | 先储能，后放能 | $\displaystyle \frac{V_o}{V_{\mathrm{in}}}=-\frac{D}{1-D}$ |
| Flyback | 隔离升降压 | 励磁电感储能 | 原边储能，副边放能 | $\displaystyle \frac{V_o}{V_{\mathrm{in}}}=\frac{N_s}{N_p}\frac{D}{1-D}$ |
| Forward | 隔离降压 | 输出电感储能，变压器传能 | 导通时直接传能 | $\displaystyle \frac{V_o}{V_{\mathrm{in}}}=\frac{N_s}{N_p}D$ |
| Half-Bridge | 隔离变换 | 变压器双极性激励 | 原边交替传能 | $\displaystyle \frac{V_o}{V_{\mathrm{in}}}=D_{\mathrm{eff}}\frac{N_s}{N_p}\frac{1}{2}$ |
| Full-Bridge | 隔离变换 | 变压器双极性激励 | 原边交替传能 | $\displaystyle \frac{V_o}{V_{\mathrm{in}}}=D_{\mathrm{eff}}\frac{N_s}{N_p}$ |
