
# 电力电子中的基础分析方法

在开始分析具体变换器之前，我们需要先建立几种基础的分析方法。

电力电子涉及开关器件、分段电路、周期波形和动态系统。如果一开始就试图求解每一个开关时刻对应的完整微分方程，分析过程很容易变得复杂，而且难以看清变换器真正的工作规律。

因此，本章不打算罗列过多数学工具，而只介绍几种在后续分析中最常使用的方法：

1. 周期稳态假设；
2. 周期平均与电感伏秒平衡、电容电荷平衡；
3. 非正弦信号的功率分析；
4. 状态空间表示；
5. 状态空间平均；
6. 周期稳态的存在性与稳定性。

在本章前半部分，我们会暂时假设变换器已经进入周期稳态，并利用这个假设简化电路分析。

在本章后半部分，我们再解释：

> 一个系统为什么可能进入周期稳态？  
> 周期稳态在什么条件下存在？  
> 即使周期稳态存在，系统又是否一定会收敛到它？

这样的安排可以让我们先掌握实用的分析方法，再讨论它背后的数学基础。

---

## 1. 周期稳态(PSS)假设

### 1.1 什么是周期稳态

设变换器的开关周期为 $T_s$。

如果系统已经进入周期稳态，那么对于系统中的每一个状态量 $x(t)$，都有

$$
x(t+T_s)=x(t)
$$

这意味着，状态量在经历一个完整的开关周期以后，会回到与周期开始时相同的值。

需要注意，周期稳态并不意味着状态量在一个周期内保持不变。

例如，电感电流可能在一个周期内先上升、后下降；电容电压也可能在一个周期内先增加、后减小。只要它们在周期结束时恰好回到周期开始时的值，系统就处于周期稳态。


---

## 2. 为什么先假设周期稳态

在实际变换器中，刚接通电源时通常会经历启动过程。电感电流、电容电压以及控制器内部状态都会从初始值逐渐变化。

经过足够长的时间以后，如果系统本身是稳定的，并且输入、负载和控制规律保持周期性，那么启动过程引起的暂态分量可能逐渐衰减，系统最终进入一种重复运行的状态。

在这种状态下：

- 每个开关周期开始时的状态相同；
- 每个周期内经历相同的开关状态；
- 电感电流和电容电压的波形周期性重复；
- 每个周期内储存的净能量变化为零。

因此，在分析变换器的稳态输入输出关系、平均值和纹波时，我们通常先作如下假设：

> 假设系统已经完成启动过程，并进入与开关周期相同的周期稳态。

这个假设使我们可以暂时忽略启动过程，只分析一个具有代表性的开关周期。

不过，这个假设并不是对所有系统都自动成立。

有些系统可能：

- 不断振荡而无法收敛；
- 状态量逐渐发散；
- 进入两倍或多倍开关周期的次谐波振荡；
- 在多个周期轨道之间跳变；
- 进入混沌状态；
- 因为输入本身缓慢变化而无法形成开关周期意义下的严格重复。

因此，我们会先把周期稳态作为一种工作假设使用，随后再利用状态转移和庞加莱映射说明它成立所需要的条件。

---

## 3. 周期平均

对于任意周期为 $T_s$ 的信号 $x(t)$，定义它在一个开关周期内的平均值为

$$
\langle x\rangle
=
\frac{1}{T_s}
\int_t^{t+T_s}x(\tau)\,\mathrm{d}\tau
$$

如果系统处于周期稳态，那么从任何相同开关相位开始计算一个完整周期，得到的平均值都是相同的。

一个周期信号可以写成

$$
x(t)
=
\langle x\rangle+\widetilde{x}(t)
$$

其中：

- $\langle x\rangle$ 是该信号的周期平均值；
- $\widetilde{x}(t)$ 是围绕平均值变化的周期纹波。

纹波部分满足

$$
\langle\widetilde{x}\rangle=0
$$

因此，平均分析的目标是把一个状态量分解为：

1. 一个反映主要工作点的平均分量；
2. 一个围绕平均值变化的纹波分量。

当纹波相对于平均值较小时，平均分量往往能够很好地描述变换器的主要工作状态。


对于电感，电容来讲，我们一般选定电感电流和电容电压作为状态变量，因为这是它们真正所“记忆”的一种状态。

---

## 4. 对 KCL 和 KVL 应用平均

基尔霍夫电流定律和基尔霍夫电压定律在任意时刻都成立。

对于某个节点，KCL 可以写成

$$
\sum_k i_k(t)=0
$$

对等式两端进行周期平均，可以得到

$$
\sum_k\langle i_k\rangle=0
$$

对于某个闭合回路，KVL 可以写成

$$
\sum_k v_k(t)=0
$$

对等式两端进行周期平均，可以得到

$$
\sum_k\langle v_k\rangle=0
$$

这是因为积分和求和都是线性运算。

但是，需要特别注意：

> 对 KCL 和 KVL 应用平均以后，真正由于周期稳态而变为零的，是状态量在一个周期内的净变化，或者等价地说，是状态量导数的周期平均值。



如果

$$
x(t+T_s)=x(t)
$$

那么

$$
\left\langle\frac{\mathrm{d}x}{\mathrm{d}t}\right\rangle
=
\frac{1}{T_s}
\int_t^{t+T_s}
\frac{\mathrm{d}x}{\mathrm{d}\tau}
\,\mathrm{d}\tau
$$

由微积分基本定理可得

$$
\left\langle\frac{\mathrm{d}x}{\mathrm{d}t}\right\rangle
=
\frac{x(t+T_s)-x(t)}{T_s}
$$

由于周期稳态满足

$$
x(t+T_s)=x(t)
$$

所以

$$
\boxed{
\left\langle\frac{\mathrm{d}x}{\mathrm{d}t}\right\rangle=0
}
$$

这一结果是电感伏秒平衡和电容电荷平衡的共同数学基础。

---

## 5. 电感伏秒平衡

电感的电压电流关系为

$$
v_L(t)
=
L\frac{\mathrm{d}i_L(t)}{\mathrm{d}t}
$$

对等式两端取一个开关周期的平均值：

$$
\langle v_L\rangle
=
L
\left\langle
\frac{\mathrm{d}i_L}{\mathrm{d}t}
\right\rangle
$$

如果电感电流处于周期稳态，那么

$$
i_L(t+T_s)=i_L(t)
$$

因此

$$
\left\langle
\frac{\mathrm{d}i_L}{\mathrm{d}t}
\right\rangle
=
0
$$

于是得到

$$
\boxed{
\langle v_L\rangle=0
}
$$

也就是说：

> 在周期稳态下，电感两端电压在一个周期内的平均值为零。

将平均值定义展开，可以写成

$$
\frac{1}{T_s}
\int_t^{t+T_s}v_L(\tau)\,\mathrm{d}\tau
=
0
$$

进一步写成

$$
\boxed{
\int_t^{t+T_s}v_L(\tau)\,\mathrm{d}\tau=0
}
$$

电压对时间的积分称为伏秒。因此，这个结果称为**电感伏秒平衡**。

它的物理含义是：

- 如果电感在某段时间内承受正电压，电感电流就会上升；
- 如果电感在另一段时间内承受负电压，电感电流就会下降；
- 在周期稳态下，一个周期内电流的总上升量必须等于总下降量。

因此，一个周期内电感获得的正伏秒必须与负伏秒相互抵消。

---

## 6. 电容电荷平衡

电容的电压电流关系为

$$
i_C(t)
=
C\frac{\mathrm{d}v_C(t)}{\mathrm{d}t}
$$

对等式两端取一个开关周期的平均值：

$$
\langle i_C\rangle
=
C
\left\langle
\frac{\mathrm{d}v_C}{\mathrm{d}t}
\right\rangle
$$

如果电容电压处于周期稳态，那么

$$
v_C(t+T_s)=v_C(t)
$$

因此

$$
\left\langle
\frac{\mathrm{d}v_C}{\mathrm{d}t}
\right\rangle
=
0
$$

于是得到

$$
\boxed{
\langle i_C\rangle=0
}
$$

也就是说：

> 在周期稳态下，流入电容的电流在一个周期内的平均值为零。

将平均值定义展开，可以写成

$$
\frac{1}{T_s}
\int_t^{t+T_s}i_C(\tau)\,\mathrm{d}\tau
=
0
$$

进一步写成

$$
\boxed{
\int_t^{t+T_s}i_C(\tau)\,\mathrm{d}\tau=0
}
$$

由于电流对时间的积分就是电荷变化量，因此，这个结果称为**电容电荷平衡**。

它的物理含义是：

- 电容电流为正时，电容获得电荷，电容电压上升；
- 电容电流为负时，电容释放电荷，电容电压下降；
- 在周期稳态下，一个周期内充入电容的电荷必须等于从电容中释放的电荷。

否则，电容电压就会在每个周期结束后发生累积变化，系统也就没有进入周期稳态。

---

## 7. 周期稳态为什么能够简化电路分析

在一个开关变换器中，不同开关状态下的电路结构通常不同。

如果直接求解完整的分段微分方程，就需要：

1. 写出每个开关状态下的微分方程；
2. 求解每一段微分方程；
3. 计算每次开关切换时的状态；
4. 将前一段的终点作为后一段的初始条件；
5. 再施加周期边界条件。

这种方法虽然完整，但对于许多基础稳态问题来说并不必要。

如果我们只希望得到平均输出电压、平均电感电流和占空比之间的关系，那么周期稳态直接给出了

$$
\langle v_L\rangle=0
$$

和

$$
\langle i_C\rangle=0
$$

借助这两个关系，再结合各个开关状态下的 KCL 和 KVL，通常就可以推导出变换器的理想稳态输入输出关系，而不必完整求解每一段微分方程。

例如，如果电感在开关导通期间承受电压 $V_{L,\mathrm{on}}$，在开关关断期间承受电压 $V_{L,\mathrm{off}}$，那么伏秒平衡给出

$$
DV_{L,\mathrm{on}}
+
(1-D)V_{L,\mathrm{off}}
=
0
$$

类似地，如果电容在开关导通和关断期间的电流分别为 $I_{C,\mathrm{on}}$ 和 $I_{C,\mathrm{off}}$，那么电荷平衡给出

$$
DI_{C,\mathrm{on}}
+
(1-D)I_{C,\mathrm{off}}
=
0
$$

通过这两个代数关系，我们往往能够快速得到变换器的平均工作点。

---

## 8. 平均分析的适用条件

周期平均是一种非常有用的方法，但不能不加条件地使用。

使用电感伏秒平衡和电容电荷平衡时，至少需要明确以下假设：

- 系统已经进入周期稳态；
- 所选择的周期确实是系统波形的重复周期；
- 输入电压和负载在一个开关周期内近似不变；
- 开关规律在分析周期内保持不变；
- 所讨论的电感电流和电容电压没有发生跨周期累积；
- 所采用的导通模式已经确定，例如连续导通模式或断续导通模式；
- 开关状态和二极管导通状态的判断正确。

还要注意：

$$
\langle xy\rangle
\neq
\langle x\rangle\langle y\rangle
$$

通常并不成立。

因此，对线性 KCL、KVL 以及线性元件方程进行平均是直接的，但当方程中出现变量乘积、非线性器件或者状态相关的开关条件时，不能随意把每个变量替换成它的平均值。

---

## 9.一个例子

利用周期稳态分析 Buck 变换器

以理想 Buck 变换器为例，说明周期稳态假设如何用于分析输入输出关系和纹波。

假设：

- 输入电压为 $V_{\mathrm{in}}$；
- 开关周期为 $T_s$，开关频率为 $f_s=1/T_s$；
- 占空比为 $D$；
- 输出端接电阻负载 $R$；
- 电感为 $L$，输出电容为 $C$；
- 变换器工作在连续导通模式；
- 输入、负载和占空比在一个周期内近似不变；
- 暂时忽略器件压降、电感电阻和电容 ESR。

选择状态变量：

$$
\boldsymbol{x}
=
\begin{bmatrix}
i_L\\
v_C
\end{bmatrix}
$$

其中，$i_L$ 是电感电流，$v_C$ 是电容电压，也是输出电压：

$$
v_o=v_C
$$

---

### 开关导通

高侧开关导通的时间为：

$$
DT_s
$$

此时输入电源通过电感向输出传递能量，电感电压为：

$$
v_{L,\mathrm{on}}
=
V_{\mathrm{in}}-V_o
$$

因此，电感电流上升：

$$
\left.
\frac{\mathrm{d}i_L}{\mathrm{d}t}
\right|_{\mathrm{on}}
=
\frac{V_{\mathrm{in}}-V_o}{L}
$$

导通期间电感电流增加量为：

$$
\Delta i_{L,\mathrm{on}}
=
\frac{V_{\mathrm{in}}-V_o}{L}DT_s
$$

---

### 开关关断

高侧开关关断的时间为：

$$
(1-D)T_s
$$

此时电感通过续流二极管或低侧同步开关继续向负载供电，电感电压为：

$$
v_{L,\mathrm{off}}
=
-V_o
$$

因此，电感电流下降：

$$
\left.
\frac{\mathrm{d}i_L}{\mathrm{d}t}
\right|_{\mathrm{off}}
=
-\frac{V_o}{L}
$$

关断期间电感电流变化量为：

$$
\Delta i_{L,\mathrm{off}}
=
-\frac{V_o}{L}(1-D)T_s
$$

---

### 利用电感伏秒平衡求输出电压

周期稳态下，电感电流在一个周期后回到原来的值：

$$
i_L(t+T_s)=i_L(t)
$$

因此：

$$
\Delta i_{L,\mathrm{on}}
+
\Delta i_{L,\mathrm{off}}
=
0
$$

代入两种状态下的变化量：

$$
\frac{V_{\mathrm{in}}-V_o}{L}DT_s
-
\frac{V_o}{L}(1-D)T_s
=
0
$$

整理得到：

$$
\boxed{
V_o=DV_{\mathrm{in}}
}
$$

这就是理想 Buck 变换器在连续导通模式下的电压变换关系。

---

### 利用电容电荷平衡求平均电流

输出节点满足：

$$
i_L=i_C+i_o
$$

周期稳态下，电容一个周期内的净电荷变化为零：

$$
\langle i_C\rangle=0
$$

因此：

$$
\boxed{
I_L=I_o
}
$$

若负载为电阻 $R$，则：

$$
I_o=\frac{V_o}{R}
$$

所以：

$$
\boxed{
I_L
=
I_o
=
\frac{V_o}{R}
}
$$

Buck 的输入电流只在高侧开关导通时存在，因此其平均值为：

$$
\boxed{
I_{\mathrm{in}}
=
DI_o
}
$$

结合

$$
V_o=DV_{\mathrm{in}}
$$

可得理想功率守恒：

$$
V_{\mathrm{in}}I_{\mathrm{in}}
=
V_oI_o
$$

---

### 电感电流纹波

在连续导通模式下，电感电流近似为三角波。为什么能这样近似？

由于输出并联的电容较大，我们粗略认为$V_o$是近似不变的，如果要考虑这种变化，得到的是复杂的指数波，而电容越大，就越趋近三角波。

其峰峰值纹波为：

$$
\Delta i_{L,\mathrm{pp}}
=
\Delta i_{L,\mathrm{on}}
$$

即：

$$
\Delta i_{L,\mathrm{pp}}
=
\frac{V_{\mathrm{in}}-V_o}{L}DT_s
$$

代入

$$
V_o=DV_{\mathrm{in}}
$$

得到：

$$
\boxed{
\Delta i_{L,\mathrm{pp}}
=
\frac{
V_{\mathrm{in}}D(1-D)
}{
Lf_s
}
}
$$

因此：

- $L$ 越大，电感电流纹波越小；
- $f_s$ 越高，电感电流纹波越小；
- 在其他参数不变时，纹波在 $D=0.5$ 附近最大。

连续导通模式要求电感电流谷值大于零：

$$
\boxed{
I_L
>
\frac{
\Delta i_{L,\mathrm{pp}}
}{2}
}
$$

---

### 输出电压纹波

输出电容电流为：

$$
i_C=i_L-i_o
$$

在小纹波近似下，$i_C$ 是平均值为零的三角波。

输出电压峰峰值纹波近似为（这通过对三角波的积分得到）：

$$
\boxed{
\Delta v_{o,\mathrm{pp}}
\approx
\frac{
\Delta i_{L,\mathrm{pp}}
}{
8Cf_s
}
}
$$

真实电容存在 ESR 时，还会产生额外纹波：

$$
\Delta v_{\mathrm{ESR,pp}}
\approx
\Delta i_{L,\mathrm{pp}}r_{\mathrm{ESR}}
$$

因此，实际输出纹波可以粗略写成：

$$
\boxed{
\Delta v_{o,\mathrm{pp}}
\approx
\frac{
\Delta i_{L,\mathrm{pp}}
}{
8Cf_s
}
+
\Delta i_{L,\mathrm{pp}}r_{\mathrm{ESR}}
}
$$


# 非正弦信号的功率分析

## 10. 为什么需要非正弦功率分析

在直流电路中，功率通常可以直接写成

$$
P=VI
$$

在正弦交流电路中，我们经常使用

$$
P
=
V_{\mathrm{rms}}I_{\mathrm{rms}}\cos\varphi
$$

其中，$\varphi$ 是电压与电流之间的相位差。

但是，电力电子变换器中存在大量开关动作，其电压和电流波形往往不是正弦波。

例如：

- 整流器的输入电流可能呈脉冲状；
- PWM 逆变器的桥臂电压是开关波形；
- 电感电流可能包含开关纹波；
- 二极管电流可能只在部分时间内存在；
- 输入电流可能含有大量谐波。

在这种情况下，只用一个相位角 $\varphi$ 已经不能完整描述电压和电流之间的关系。

因此，我们需要引入：

- 平均有功功率；
- 有效值；
- 视在功率；
- 功率因数；
- 位移因数；
- 畸变因数；
- 总谐波畸变率。

---

## 11. 非正弦条件下的有功功率

无论电压和电流是否为正弦波，瞬时功率始终定义为

$$
p(t)
=
v(t)i(t)
$$

如果电压和电流以 $T$ 为周期，那么平均有功功率定义为

$$
\boxed{
P
=
\frac{1}{T}
\int_t^{t+T}
v(\tau)i(\tau)\,\mathrm{d}\tau
}
$$

这个定义对直流、正弦交流和非正弦周期信号都成立。

需要注意：

$$
P
\neq
V_{\mathrm{avg}}I_{\mathrm{avg}}
$$

通常也不能写成

$$
P
=
V_{\mathrm{rms}}I_{\mathrm{rms}}
$$

后一个乘积表示的是视在功率，而不是一定等于有功功率。

---

## 12. 有效值与视在功率

周期电压的有效值定义为

$$
V_{\mathrm{rms}}
=
\sqrt{
\frac{1}{T}
\int_t^{t+T}v^2(\tau)\,\mathrm{d}\tau
}
$$

周期电流的有效值定义为

$$
I_{\mathrm{rms}}
=
\sqrt{
\frac{1}{T}
\int_t^{t+T}i^2(\tau)\,\mathrm{d}\tau
}
$$

视在功率定义为

$$
\boxed{
S
=
V_{\mathrm{rms}}I_{\mathrm{rms}}
}
$$

单位为伏安，即 $\mathrm{VA}$。

视在功率反映电源和导线需要承受的总体电压、电流容量，而有功功率 $P$ 反映真正传递给负载并转化为机械能、热能或其他形式的平均能量。

---

## 13. 功率因数

功率因数定义为

$$
\boxed{
\mathrm{PF}
=
\frac{P}{S}
=
\frac{P}
{V_{\mathrm{rms}}I_{\mathrm{rms}}}
}
$$

对于一般周期波形，这个定义始终成立。

功率因数越接近 $1$，说明在传递相同有功功率时，所需要的电流有效值越小。

如果功率因数较低，那么为了传递相同的有功功率，电源、导线、开关器件和滤波器通常需要承受更大的电流，从而增加：

- 导线损耗；
- 器件导通损耗；
- 配电容量；
- 电磁干扰；
- 电网电流畸变。

功率因数降低可能来自两种不同原因：

1. 电压和电流基波之间存在相位差；
2. 电流中含有较多谐波。

这两种原因分别对应位移和畸变。

---

## 14. 谐波分解

周期信号可以表示为基波与各次谐波之和。

设电压和电流分别表示为

$$
v(t)
=
V_0
+
\sqrt{2}
\sum_{n=1}^{\infty}
V_n
\cos(n\omega_0t+\alpha_n)
$$

$$
i(t)
=
I_0
+
\sqrt{2}
\sum_{n=1}^{\infty}
I_n
\cos(n\omega_0t+\beta_n)
$$

其中：

- $V_0$ 和 $I_0$ 是直流分量；
- $V_n$ 和 $I_n$ 是第 $n$ 次谐波的有效值；
- $\alpha_n$ 和 $\beta_n$ 是对应谐波的相位；
- $\omega_0$ 是基波角频率。

利用不同频率正弦信号之间的正交性，平均有功功率可以写成

$$
P
=
V_0I_0
+
\sum_{n=1}^{\infty}
V_nI_n\cos\varphi_n
$$

其中

$$
\varphi_n
=
\alpha_n-\beta_n
$$

这说明：

> 只有电压与电流中频率相同的分量，才会共同产生非零的平均有功功率。

不同次数的电压谐波和电流谐波在一个完整基波周期内的积分为零。

---

## 15. 位移因数与畸变因数

如果电源电压近似为理想正弦波，并且没有明显直流分量，那么电压主要只包含基波。

此时，有功功率为

$$
P
=
V_{1,\mathrm{rms}}
I_{1,\mathrm{rms}}
\cos\varphi_1
$$

其中：

- $V_{1,\mathrm{rms}}$ 是电压基波有效值；
- $I_{1,\mathrm{rms}}$ 是电流基波有效值；
- $\varphi_1$ 是电压基波与电流基波之间的相位差。

由于电压近似为纯正弦波，所以

$$
V_{\mathrm{rms}}
=
V_{1,\mathrm{rms}}
$$

于是功率因数可以写成

$$
\mathrm{PF}
=
\frac{
V_{1,\mathrm{rms}}
I_{1,\mathrm{rms}}
\cos\varphi_1
}{
V_{1,\mathrm{rms}}
I_{\mathrm{rms}}
}
$$

整理得到

$$
\boxed{
\mathrm{PF}
=
\frac{I_{1,\mathrm{rms}}}{I_{\mathrm{rms}}}
\cos\varphi_1
}
$$

定义位移因数为

$$
\boxed{
\mathrm{DPF}
=
\cos\varphi_1
}
$$

定义畸变因数为

$$
\boxed{
\mathrm{DF}
=
\frac{I_{1,\mathrm{rms}}}{I_{\mathrm{rms}}}
}
$$

因此，在电源电压近似为正弦波时，

$$
\boxed{
\mathrm{PF}
=
\mathrm{DPF}\times\mathrm{DF}
}
$$

也就是

$$
\boxed{
\mathrm{PF}
=
\cos\varphi_1
\frac{I_{1,\mathrm{rms}}}{I_{\mathrm{rms}}}
}
$$

这个表达式说明：

- 即使电流基波与电压完全同相，只要电流含有大量谐波，功率因数仍然会降低；
- 即使电流波形接近正弦，只要它与电压之间存在明显相位差，功率因数同样会降低。

---

## 16. 总谐波畸变率

电流总谐波畸变率定义为

$$
\boxed{
\mathrm{THD}_I
=
\frac{
\sqrt{
I_{2,\mathrm{rms}}^2
+
I_{3,\mathrm{rms}}^2
+
I_{4,\mathrm{rms}}^2
+\cdots
}
}{
I_{1,\mathrm{rms}}
}
}
$$

也可以写成

$$
\mathrm{THD}_I
=
\frac{
\sqrt{
\displaystyle\sum_{n=2}^{\infty}I_{n,\mathrm{rms}}^2
}
}{
I_{1,\mathrm{rms}}
}
$$

由各次谐波的正交性可得

$$
I_{\mathrm{rms}}^2
=
I_{1,\mathrm{rms}}^2
+
\sum_{n=2}^{\infty}I_{n,\mathrm{rms}}^2
$$

因此

$$
I_{\mathrm{rms}}^2
=
I_{1,\mathrm{rms}}^2
\left(
1+\mathrm{THD}_I^2
\right)
$$

于是

$$
\frac{I_{1,\mathrm{rms}}}{I_{\mathrm{rms}}}
=
\frac{1}{
\sqrt{1+\mathrm{THD}_I^2}
}
$$

所以，在电源电压近似为正弦波时，

$$
\boxed{
\mathrm{DF}
=
\frac{1}{
\sqrt{1+\mathrm{THD}_I^2}
}
}
$$

进一步得到

$$
\boxed{
\mathrm{PF}
=
\frac{\cos\varphi_1}{
\sqrt{1+\mathrm{THD}_I^2}
}
}
$$

这个公式清楚地说明了位移和畸变对功率因数的共同影响。

但是，这个分解建立在电源电压近似为正弦波的条件下。如果电压本身也含有明显谐波，就应当回到一般定义

$$
\mathrm{PF}
=
\frac{P}{
V_{\mathrm{rms}}I_{\mathrm{rms}}
}
$$

并根据各次电压、电流谐波共同计算有功功率，而不能简单套用上述公式。

---

# 更高阶的数学工具

以下内容需要一定的数学基础和耐心，并且对你初级阶段的设计，理解没有非常大的帮助，但如果你真的好奇为什么一个系统能这么分析，好奇一个系统该怎么控制，好奇怎么用数学工具得到这个电路具体的每个部分的电流，电压随时间的函数。那么还是可以阅读一下的。


## 17. 为什么需要状态空间理论

周期稳态和平均平衡可以快速得到变换器的稳态工作点，但它们不能直接解决所有问题。

例如：

- 启动过程中电感电流如何变化；
- 负载突然变化后输出电压如何恢复；
- 控制器是否会导致振荡；
- 某个扰动会以多快的速度衰减；
- 系统是否能够进入周期稳态；
- 占空比变化如何影响输出。

这些问题都与系统的动态过程有关，需要使用微分方程描述。

状态空间理论可以看作矩阵理论在线性常微分方程组上的应用。它把多个相互耦合的微分方程组织成一个统一的向量方程。

---

## 18. 状态变量

所谓状态，是指一组能够结合当前输入，完整决定系统未来运动的最小变量。

在电路中，状态变量通常选择为：

- 电感电流；
- 电容电压。

原因是电感和电容能够储存能量，并具有记忆性。

对于一个具有 $m$ 个独立电感和 $n$ 个独立电容的电路，可以将状态变量写成向量

$$
\boldsymbol{x}(t)
=
\begin{bmatrix}
i_{L1}(t)\\
i_{L2}(t)\\
\vdots\\
v_{C1}(t)\\
v_{C2}(t)\\
\vdots
\end{bmatrix}
$$

将输入量写成向量

$$
\boldsymbol{u}(t)
=
\begin{bmatrix}
u_1(t)\\
u_2(t)\\
\vdots
\end{bmatrix}
$$

将需要观察的输出量写成向量

$$
\boldsymbol{y}(t)
=
\begin{bmatrix}
y_1(t)\\
y_2(t)\\
\vdots
\end{bmatrix}
$$

一个线性时不变系统可以写成

$$
\boxed{
\dot{\boldsymbol{x}}
=
\boldsymbol{A}\boldsymbol{x}
+
\boldsymbol{B}\boldsymbol{u}
}
$$

$$
\boxed{
\boldsymbol{y}
=
\boldsymbol{C}\boldsymbol{x}
+
\boldsymbol{D}\boldsymbol{u}
}
$$

其中：

- $\boldsymbol{A}$ 描述状态之间的相互作用；
- $\boldsymbol{B}$ 描述输入如何影响状态；
- $\boldsymbol{C}$ 描述如何从状态计算输出；
- $\boldsymbol{D}$ 描述输入是否直接影响输出。

---

## 19. 状态方程的解

对于线性时不变系统

$$
\dot{\boldsymbol{x}}
=
\boldsymbol{A}\boldsymbol{x}
+
\boldsymbol{B}\boldsymbol{u}
$$

从初始时刻 $t_0$ 到时刻 $t$ 的解为

$$
\boldsymbol{x}(t)
=
e^{\boldsymbol{A}(t-t_0)}
\boldsymbol{x}(t_0)
+
\int_{t_0}^{t}
e^{\boldsymbol{A}(t-\tau)}
\boldsymbol{B}\boldsymbol{u}(\tau)
\,\mathrm{d}\tau
$$

其中，矩阵指数

$$
e^{\boldsymbol{A}t}
$$

描述系统的初始状态如何随时间传播。

这个表达式可以分成两部分：

1. 初始状态引起的自由响应；
2. 外部输入引起的受迫响应。

这实际上就是高数中的一阶线性微分方程，不过原本的普通指数变为了矩阵指数。

---

# 开关变换器的状态空间平均

## 20. 开关变换器为什么不能直接写成一个固定的状态方程

开关变换器在不同开关状态下具有不同的电路结构。

以具有两个开关状态的变换器为例。

当开关处于状态 1 时，电路的状态方程可以写成

$$
\dot{\boldsymbol{x}}
=
\boldsymbol{A}_1\boldsymbol{x}
+
\boldsymbol{B}_1\boldsymbol{u}
$$

输出方程为

$$
\boldsymbol{y}
=
\boldsymbol{C}_1\boldsymbol{x}
+
\boldsymbol{D}_1\boldsymbol{u}
$$

当开关处于状态 2 时，电路的状态方程变为

$$
\dot{\boldsymbol{x}}
=
\boldsymbol{A}_2\boldsymbol{x}
+
\boldsymbol{B}_2\boldsymbol{u}
$$

输出方程变为

$$
\boldsymbol{y}
=
\boldsymbol{C}_2\boldsymbol{x}
+
\boldsymbol{D}_2\boldsymbol{u}
$$

虽然每一个固定开关状态下的电路都可能是线性的，但整个变换器需要在多组状态方程之间不断切换。

因此，更准确地说，开关变换器是一个分段线性系统、切换系统或者混合系统，而不能直接用一组固定的线性时不变方程描述。

这也是开关模型难以直接用于传统线性控制器设计的原因。

---

## 21. 状态空间平均的基本思想

设状态 1 在一个周期内持续的时间为

$$
DT_s
$$

状态 2 持续的时间为

$$
(1-D)T_s
$$

如果状态变量在一个开关周期内的变化不大，并且输入量在一个周期内近似保持不变，那么可以用两个状态方程按照持续时间进行加权平均。

状态 1 的权重为 $D$，状态 2 的权重为 $1-D$。

于是得到平均状态方程

$$
\dot{\overline{\boldsymbol{x}}}
=
D
\left(
\boldsymbol{A}_1\overline{\boldsymbol{x}}
+
\boldsymbol{B}_1\overline{\boldsymbol{u}}
\right)
+
(1-D)
\left(
\boldsymbol{A}_2\overline{\boldsymbol{x}}
+
\boldsymbol{B}_2\overline{\boldsymbol{u}}
\right)
$$

整理得到

$$
\boxed{
\dot{\overline{\boldsymbol{x}}}
=
\overline{\boldsymbol{A}}
\overline{\boldsymbol{x}}
+
\overline{\boldsymbol{B}}
\overline{\boldsymbol{u}}
}
$$

其中

$$
\boxed{
\overline{\boldsymbol{A}}
=
D\boldsymbol{A}_1
+
(1-D)\boldsymbol{A}_2
}
$$

$$
\boxed{
\overline{\boldsymbol{B}}
=
D\boldsymbol{B}_1
+
(1-D)\boldsymbol{B}_2
}
$$

类似地，平均输出方程为

$$
\boxed{
\overline{\boldsymbol{y}}
=
\overline{\boldsymbol{C}}
\overline{\boldsymbol{x}}
+
\overline{\boldsymbol{D}}
\overline{\boldsymbol{u}}
}
$$

其中

$$
\overline{\boldsymbol{C}}
=
D\boldsymbol{C}_1
+
(1-D)\boldsymbol{C}_2
$$

$$
\overline{\boldsymbol{D}}
=
D\boldsymbol{D}_1
+
(1-D)\boldsymbol{D}_2
$$

这就是状态空间平均的基本形式。

---

## 22. 为什么可以按照占空比平均

状态空间平均是一个开关周期内状态变化的一阶近似。

为了看清这一点，暂时考虑无外部输入的系统。

在状态 1 中，系统运行时间为 $DT_s$，因此

$$
\boldsymbol{x}(DT_s)
=
e^{\boldsymbol{A}_1DT_s}
\boldsymbol{x}(0)
$$

随后，系统进入状态 2，并运行 $(1-D)T_s$，于是一个完整周期后的状态为

$$
\boldsymbol{x}(T_s)
=
e^{\boldsymbol{A}_2(1-D)T_s}
e^{\boldsymbol{A}_1DT_s}
\boldsymbol{x}(0)
$$

因此，一个周期的精确状态转移矩阵为

$$
\boldsymbol{\Phi}
=
e^{\boldsymbol{A}_2(1-D)T_s}
e^{\boldsymbol{A}_1DT_s}
$$

当 $T_s$ 足够小时，可以使用矩阵指数的一阶展开：

$$
e^{\boldsymbol{A}_1DT_s}
=
\boldsymbol{I}
+
\boldsymbol{A}_1DT_s
+
O(T_s^2)
$$

$$
e^{\boldsymbol{A}_2(1-D)T_s}
=
\boldsymbol{I}
+
\boldsymbol{A}_2(1-D)T_s
+
O(T_s^2)
$$

将两式相乘：

$$
\boldsymbol{\Phi}
=
\left[
\boldsymbol{I}
+
\boldsymbol{A}_2(1-D)T_s
\right]
\left[
\boldsymbol{I}
+
\boldsymbol{A}_1DT_s
\right]
+
O(T_s^2)
$$

保留关于 $T_s$ 的一阶项，得到

$$
\boldsymbol{\Phi}
=
\boldsymbol{I}
+
\left[
D\boldsymbol{A}_1
+
(1-D)\boldsymbol{A}_2
\right]T_s
+
O(T_s^2)
$$

因此，在忽略 $O(T_s^2)$ 以及更高阶项时，一个周期内的状态变化等效于由平均矩阵

$$
\overline{\boldsymbol{A}}
=
D\boldsymbol{A}_1
+
(1-D)\boldsymbol{A}_2
$$

产生。

这说明，状态空间平均成立的关键在于，周期足够短让我们能够忽略高次项


---

## 23. 平均模型一定是线性系统吗

如果占空比 $D$ 是一个固定常数，输入电压也作为外部输入处理，并且每个开关状态下的电路都是线性的，那么平均模型可以写成线性状态空间形式：

$$
\dot{\overline{\boldsymbol{x}}}
=
\overline{\boldsymbol{A}}
\overline{\boldsymbol{x}}
+
\overline{\boldsymbol{B}}
\overline{\boldsymbol{u}}
$$

但是，在闭环变换器中，占空比通常不是固定常数，而是控制器根据状态和输出计算得到的变量。

如果

$$
D=D(\boldsymbol{x},\boldsymbol{u},t)
$$

那么平均矩阵也会依赖状态、输入或者时间：

$$
\overline{\boldsymbol{A}}
=
D\boldsymbol{A}_1
+
(1-D)\boldsymbol{A}_2
$$

此时，平均模型可能仍然是非线性的或者时变的。

因此：

> 状态空间平均消除了显式的高频开关切换，但并不保证所得模型自动成为线性时不变系统。

为了使用经典线性控制理论，通常还需要在某个稳态工作点附近进行小信号线性化。

---

## 24. 稳态工作点与小信号线性化

设平均状态、输入和占空比分别由稳态量与小扰动组成：

$$
\overline{\boldsymbol{x}}
=
\boldsymbol{X}
+
\widehat{\boldsymbol{x}}
$$

$$
\overline{\boldsymbol{u}}
=
\boldsymbol{U}
+
\widehat{\boldsymbol{u}}
$$

$$
D
=
D_0+\widehat{d}
$$

其中：

- $\boldsymbol{X}$ 是稳态工作点；
- $\boldsymbol{U}$ 是稳态输入；
- $D_0$ 是稳态占空比；
- $\widehat{\boldsymbol{x}}$ 是状态的小扰动；
- $\widehat{\boldsymbol{u}}$ 是输入的小扰动；
- $\widehat{d}$ 是占空比的小扰动。

稳态工作点满足

$$
\boldsymbol{0}
=
\overline{\boldsymbol{A}}_0\boldsymbol{X}
+
\overline{\boldsymbol{B}}_0\boldsymbol{U}
$$

在这个工作点附近展开平均方程，并忽略两个小扰动乘积形成的二阶小量，可以得到小信号模型

$$
\boxed{
\dot{\widehat{\boldsymbol{x}}}
=
\boldsymbol{A}_{\mathrm{ss}}
\widehat{\boldsymbol{x}}
+
\boldsymbol{B}_{\mathrm{ss}}
\widehat{\boldsymbol{u}}
+
\boldsymbol{E}_{\mathrm{ss}}
\widehat{d}
}
$$

相应的输出方程为

$$
\boxed{
\widehat{\boldsymbol{y}}
=
\boldsymbol{C}_{\mathrm{ss}}
\widehat{\boldsymbol{x}}
+
\boldsymbol{D}_{\mathrm{ss}}
\widehat{\boldsymbol{u}}
+
\boldsymbol{F}_{\mathrm{ss}}
\widehat{d}
}
$$

这时，我们才得到一组可以直接使用线性控制理论分析的方程。

利用这个模型，可以研究：

- 占空比到输出电压的传递关系；
- 输入电压扰动到输出的传递关系；
- 负载扰动到输出的传递关系；
- 系统的极点和零点；
- 闭环稳定性；
- 控制器参数设计。


---

# 周期稳态为什么存在

## 25. 从一个周期观察系统

前面我们直接假设系统已经进入周期稳态。

现在回过头来讨论，这个周期稳态为什么可能存在。

设每个开关周期开始时对系统状态进行一次采样：

$$
\boldsymbol{x}_n
=
\boldsymbol{x}(nT_s)
$$

系统经历一个完整的开关周期以后，状态变为

$$
\boldsymbol{x}_{n+1}
=
\boldsymbol{x}((n+1)T_s)
$$

一个周期内所有连续时间演化和开关切换的综合效果，都可以用一个映射表示：

$$
\boxed{
\boldsymbol{x}_{n+1}
=
\boldsymbol{F}(\boldsymbol{x}_n)
}
$$

这个映射称为庞加莱映射，也称为周期映射或采样映射。

它把一个周期开始时的状态映射到下一个周期开始时的状态。

---

## 26. 周期稳态对应不动点

如果系统处于与开关周期相同的周期稳态，那么一个周期开始时和下一个周期开始时的状态相同：

$$
\boldsymbol{x}_{n+1}
=
\boldsymbol{x}_n
=
\boldsymbol{x}^\ast
$$

代入庞加莱映射可得

$$
\boxed{
\boldsymbol{x}^\ast
=
\boldsymbol{F}(\boldsymbol{x}^\ast)
}
$$

满足这个方程的状态 $\boldsymbol{x}^\ast$ 称为映射 $\boldsymbol{F}$ 的不动点。

因此：

> 寻找变换器的周期稳态，本质上就是寻找一个周期状态转移映射的不动点。

需要区分两个问题：

1. 不动点是否存在；
2. 系统是否会从附近的初始状态收敛到这个不动点。

不动点存在，只说明系统具有一个可能的周期运行轨道，并不能自动保证实际系统一定会进入这个周期轨道。

---

## 27. 线性仿射周期映射

对于一个开关顺序和占空比固定的分段线性系统，一个周期的状态转移通常可以写成

$$
\boxed{
\boldsymbol{x}_{n+1}
=
\boldsymbol{\Phi}\boldsymbol{x}_n
+
\boldsymbol{\Gamma}
}
$$

其中：

- $\boldsymbol{\Phi}$ 是一个周期的状态转移矩阵；
- $\boldsymbol{\Gamma}$ 表示周期输入在一个周期内产生的累计作用。

如果存在周期稳态 $\boldsymbol{x}^\ast$，则有

$$
\boldsymbol{x}^\ast
=
\boldsymbol{\Phi}\boldsymbol{x}^\ast
+
\boldsymbol{\Gamma}
$$

整理得到

$$
\left(
\boldsymbol{I}-\boldsymbol{\Phi}
\right)
\boldsymbol{x}^\ast
=
\boldsymbol{\Gamma}
$$

如果矩阵

$$
\boldsymbol{I}-\boldsymbol{\Phi}
$$

可逆，那么周期稳态唯一，并且可以写成

$$
\boxed{
\boldsymbol{x}^\ast
=
\left(
\boldsymbol{I}-\boldsymbol{\Phi}
\right)^{-1}
\boldsymbol{\Gamma}
}
$$

这说明了线性仿射系统中周期稳态不动点存在和唯一的一种基本条件。

---

## 28. 周期稳态是否稳定

定义第 $n$ 个周期开始时的状态误差为

$$
\boldsymbol{e}_n
=
\boldsymbol{x}_n-\boldsymbol{x}^\ast
$$

因为

$$
\boldsymbol{x}_{n+1}
=
\boldsymbol{\Phi}\boldsymbol{x}_n
+
\boldsymbol{\Gamma}
$$

并且

$$
\boldsymbol{x}^\ast
=
\boldsymbol{\Phi}\boldsymbol{x}^\ast
+
\boldsymbol{\Gamma}
$$

两式相减，得到

$$
\boldsymbol{e}_{n+1}
=
\boldsymbol{\Phi}\boldsymbol{e}_n
$$

经过 $n$ 个周期以后，

$$
\boldsymbol{e}_n
=
\boldsymbol{\Phi}^n\boldsymbol{e}_0
$$

因此，系统是否收敛到周期稳态，取决于

$$
\boldsymbol{\Phi}^n
$$

是否随着 $n$ 增大而趋近于零。

如果状态转移矩阵 $\boldsymbol{\Phi}$ 的所有特征值都位于单位圆内部，即

$$
\boxed{
\rho(\boldsymbol{\Phi})<1
}
$$

其中 $\rho(\boldsymbol{\Phi})$ 表示谱半径，那么

$$
\boldsymbol{\Phi}^n\rightarrow\boldsymbol{0}
$$

从而

$$
\boldsymbol{e}_n\rightarrow\boldsymbol{0}
$$

这说明，无论初始状态与周期稳态之间存在怎样的小偏差，这个偏差都会在多次周期迭代中逐渐减小，系统最终收敛到周期稳态。

如果某个特征值的模大于 $1$，那么对应方向上的误差会逐周期放大，周期稳态不稳定。

如果某个特征值的模等于 $1$，误差可能既不衰减也不增大，系统不一定能够自行收敛到唯一的周期稳态。

---

## 29. 非线性系统的局部稳定性

对于一般的非线性庞加莱映射

$$
\boldsymbol{x}_{n+1}
=
\boldsymbol{F}(\boldsymbol{x}_n)
$$

设 $\boldsymbol{x}^\ast$ 是一个不动点：

$$
\boldsymbol{x}^\ast
=
\boldsymbol{F}(\boldsymbol{x}^\ast)
$$

在不动点附近进行线性化：

$$
\boldsymbol{e}_{n+1}
\approx
\boldsymbol{J}
\boldsymbol{e}_n
$$

其中

$$
\boldsymbol{J}
=
\left.
\frac{\partial\boldsymbol{F}}
{\partial\boldsymbol{x}}
\right|_{\boldsymbol{x}=\boldsymbol{x}^\ast}
$$

是庞加莱映射在不动点处的雅可比矩阵。

如果 $\boldsymbol{J}$ 的所有特征值都位于单位圆内部，即

$$
\rho(\boldsymbol{J})<1
$$

那么这个周期稳态在局部是渐近稳定的。

这里的“局部”意味着：只有当初始状态距离周期轨道足够近时，才能保证误差逐渐减小。

如果希望证明更大范围甚至全局范围内的收敛，通常需要更强的条件。

例如，如果在某个区域内存在常数 $0\leq q<1$，使得

$$
\left\|
\boldsymbol{F}(\boldsymbol{x})
-
\boldsymbol{F}(\boldsymbol{y})
\right\|
\leq
q
\left\|
\boldsymbol{x}-\boldsymbol{y}
\right\|
$$

那么 $\boldsymbol{F}$ 在这个区域内是压缩映射。

根据压缩映射原理，该区域内存在唯一不动点，并且从区域内任意初始状态出发的迭代都会收敛到这个不动点。

---

## 30. 为什么实际变换器经常能够进入周期稳态

在真实变换器中，电阻、负载和器件损耗会消耗扰动能量。

例如：

- 电感的绕组电阻会耗散能量；
- 电容的等效串联电阻会耗散能量；
- 负载会不断吸收能量；
- 功率器件的导通损耗会耗散能量；
- 稳定的闭环控制器会抑制状态偏差。

这些因素可能使系统在每一个周期以后都遗忘一部分初始状态，使误差逐周期减小。

从庞加莱映射的角度看，这相当于状态转移对误差具有收缩作用。

因此，许多具有适当阻尼并经过稳定设计的变换器，在固定输入、固定负载和周期控制条件下，确实会经过启动暂态后进入周期稳态。

但是，这仍然不是无条件成立的。

例如，一个完全理想、没有任何损耗的 LC 网络可能长期保留由初始条件引起的自由振荡，而不自动收敛到唯一的周期轨道。

如果控制器设计不稳定，状态误差还可能逐周期增大。


---

## 31. 更长周期的稳态

系统不一定总是以开关周期 $T_s$ 为周期重复。

有时可能出现

$$
\boldsymbol{x}(t+2T_s)
=
\boldsymbol{x}(t)
$$

但是

$$
\boldsymbol{x}(t+T_s)
\neq
\boldsymbol{x}(t)
$$

这表示系统进入了两倍开关周期的运行状态。

类似地，还可能出现三倍周期、四倍周期以及更加复杂的次谐波振荡。

在庞加莱映射中，一倍周期稳态是不动点：

$$
\boldsymbol{F}(\boldsymbol{x}^\ast)
=
\boldsymbol{x}^\ast
$$

两倍周期稳态则满足

$$
\boldsymbol{F}^2(\boldsymbol{x}^\ast)
=
\boldsymbol{x}^\ast
$$

但不满足

$$
\boldsymbol{F}(\boldsymbol{x}^\ast)
=
\boldsymbol{x}^\ast
$$

因此，在使用周期稳态假设之前，需要确认系统的真实重复周期。

如果系统已经出现次谐波振荡，就不能继续把一个开关周期内的状态净变化直接设为零，而应当使用完整的实际重复周期进行平均。
